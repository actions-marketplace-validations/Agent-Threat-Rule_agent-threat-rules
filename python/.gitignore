# Rule bundle generated by scripts/bundle_rules.py at build time — not committed
# (regenerated from the canonical rules/ dir on every build, so it never drifts)
pyatr/_bundled_rules.json

# Build / packaging artifacts
build/
dist/
*.egg-info/
__pycache__/
.venv/
.pytest_cache/

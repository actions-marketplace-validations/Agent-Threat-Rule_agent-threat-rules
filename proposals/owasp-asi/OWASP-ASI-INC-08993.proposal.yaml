# ATR rule proposal -- generated by scripts/sync-owasp-asi.ts
# Source: OWASP Agentic AI Security Incidents tracker (incident INC-08993)
# Imported on: 2026-06-18
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The incident narrative
#         is attached as a YAML comment under detection.conditions for ease of
#         conversion to a precision-safe matcher (most incidents are prose, so
#         detection_ready is usually false -- tracking-only until a payload is
#         identified).
# Detection readiness: READY -- inline payload / code block in incident body
title: "AVideo has Stored XSS via Unescaped Menu Item Fields in TopMenu Plugin"
id: ATR-DRAFT-OWASP-ASI-INC-08993
rule_version: 1
status: draft
description: >
  OWASP Agentic AI Security Incident INC-08993.
  OWASP ASI: ASI04, ASI05.
  ## Summary The TopMenu plugin renders menu item fields (icon classes, URLs, and text labels) directly into HTML without applying `htmlspecialchars()` or any other output encoding. Since menu items are rendered on every public page through plugin hooks, a single malicious menu entry results in stored cross-site scripting that executes for every visitor to the site. An admin user who is tricked into saving a crafted menu item (or an attacker who gains admin access) can compromise all site visitors. ## Details Multiple output locations in the TopMenu plugin render user-controlled data without esc
author: "ATR Community (OWASP-ASI sync)"
date: "2026/06/18"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: medium

references:
  cwe:
    - "CWE-79"
  external:
    - "https://github.com/advisories/GHSA-gmpc-fxg2-vcmq"
    - "https://github.com/WWBN/AVideo/security/advisories/GHSA-gmpc-fxg2-vcmq"
    - "https://owasp-agentic-ai-security-incidents.lovable.app/"

metadata_provenance:
  owasp_asi: owasp-asi-sync
  cwe: owasp-asi-sync

tags:
  category: tool-poisoning
  subcategory: owasp-asi-incident
  scan_target: runtime
  confidence: high

agent_source:
  type: llm_io
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the incident narrative below into precision-safe
    # detection.conditions regex/string matchers. Without this work the rule
    # will neither fire nor be promotable to rules/.
    #
    # === OWASP-ASI incident narrative ===
    # ## Summary The TopMenu plugin renders menu item fields (icon classes, URLs, and text labels) directly into HTML without applying `htmlspecialchars()` or any other output encoding. Since menu items are rendered on every public page through plugin hooks, a single malicious menu entry results in stored cross-site scripting that executes for every visitor to the site. An admin user who is tricked into saving a crafted menu item (or an attacker who gains admin access) can compromise all site visitors. ## Details Multiple output locations in the TopMenu plugin render user-controlled data without escaping: In `HTMLMenuRight.php:24`, the icon class is injected directly: ```php <i class="<?php echo $value2['icon'] ?>"></i> ``` In `HTMLMenuRight.php:40`, the URL is rendered without encoding: ```php <a href="<?php echo $value2['finalURL']; ?>"> ``` In `HTMLMenuLeft.php:32`, same pattern for the left menu: ```php <a href="<?php echo $value2['finalURL']; ?>"> ``` In `index.php:49`, the menu item text is echoed raw: ```php <?php echo $menuItem->getText(); ?> ``` Menu item data is saved via `menuItemSave.json.php` with no sanitization in the setter methods. The stored values are loaded from the database and rendered on every page because the TopMenu plugin hooks into the global page layout. Critically, `menuItemSave.json.php` has no CSRF protection. It checks `User::isAdmin()` but does not call `isGlobalTokenValid()` or perform any other CSRF token validation. This means the st
    # Attack vector: xss
    # Affected: composer/wwbn/avideo
    []

response:
  actions:
    - alert
  notify:
    - security_team

confidence: 70

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: true
  reason: "inline payload / code block in incident body"

# === sync-owasp-asi.ts provenance ===
_owasp_asi_sync:
  incident_id: "INC-08993"
  cve_ids: []
  cwe_ids: ["CWE-79"]
  owasp_asi: ["ASI04","ASI05"]
  owasp_llm: ["LLM03","LLM05"]
  mitre_atlas: ["AML.T0010","AML.T0050"]
  attack_vector: "xss"
  affected: "composer/wwbn/avideo"
  severity: "Medium"
  category: "vulnerability-disclosure"
  tier: "feed"
  quality_tier: "reviewed"
  incident_date: "2026-04"
  tracker_url: "https://owasp-agentic-ai-security-incidents.lovable.app/"
  imported_at: "2026-06-18T10:15:28.467Z"
  data_source_used: "emmanuelgjr/genai_incidents data/incidents.json"
  advisory_body: |
    ## Summary The TopMenu plugin renders menu item fields (icon classes, URLs, and text labels) directly into HTML without applying `htmlspecialchars()` or any other output encoding. Since menu items are rendered on every public page through plugin hooks, a single malicious menu entry results in stored cross-site scripting that executes for every visitor to the site. An admin user who is tricked into saving a crafted menu item (or an attacker who gains admin access) can compromise all site visitors. ## Details Multiple output locations in the TopMenu plugin render user-controlled data without escaping: In `HTMLMenuRight.php:24`, the icon class is injected directly: ```php <i class="<?php echo $value2['icon'] ?>"></i> ``` In `HTMLMenuRight.php:40`, the URL is rendered without encoding: ```php <a href="<?php echo $value2['finalURL']; ?>"> ``` In `HTMLMenuLeft.php:32`, same pattern for the left menu: ```php <a href="<?php echo $value2['finalURL']; ?>"> ``` In `index.php:49`, the menu item text is echoed raw: ```php <?php echo $menuItem->getText(); ?> ``` Menu item data is saved via `menuItemSave.json.php` with no sanitization in the setter methods. The stored values are loaded from the database and rendered on every page because the TopMenu plugin hooks into the global page layout. Critically, `menuItemSave.json.php` has no CSRF protection. It checks `User::isAdmin()` but does not call `isGlobalTokenValid()` or perform any other CSRF token validation. This means the st
    
    Attack vector: xss
    
    Affected: composer/wwbn/avideo

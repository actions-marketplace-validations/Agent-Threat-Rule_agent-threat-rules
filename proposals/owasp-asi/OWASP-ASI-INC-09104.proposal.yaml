# ATR rule proposal -- generated by scripts/sync-owasp-asi.ts
# Source: OWASP Agentic AI Security Incidents tracker (incident INC-09104)
# Imported on: 2026-06-18
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The incident narrative
#         is attached as a YAML comment under detection.conditions for ease of
#         conversion to a precision-safe matcher (most incidents are prose, so
#         detection_ready is usually false -- tracking-only until a payload is
#         identified).
# Detection readiness: READY -- inline payload / code block in incident body
title: "@grackle-ai/server: Unescaped Error String in renderPairingPage() HTML Template"
id: ATR-DRAFT-OWASP-ASI-INC-09104
rule_version: 1
status: draft
description: >
  OWASP Agentic AI Security Incident INC-09104.
  OWASP ASI: ASI04, ASI05.
  ### Impact The `renderPairingPage()` function embeds the `error` parameter directly into HTML without escaping: ```typescript const errorHtml = error ? `<p style="color:#e74c3c">${error}</p>` : ""; ``` All current call sites pass hardcoded strings, so this is **not exploitable today**. However, the function is architecturally fragile — if a future code change passes user-controlled or dynamic content into the error parameter, it would create an XSS vulnerability. The `renderAuthorizePage()` function in the same file correctly uses `escapeHtml()` for dynamic content, making this an inconsistenc
author: "ATR Community (OWASP-ASI sync)"
date: "2026/06/18"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: low

references:
  cwe:
    - "CWE-79"
  external:
    - "https://github.com/advisories/GHSA-7q9x-8g6p-3x75"
    - "https://github.com/nick-pape/grackle/security/advisories/GHSA-7q9x-8g6p-3x75"
    - "https://owasp-agentic-ai-security-incidents.lovable.app/"

metadata_provenance:
  owasp_asi: owasp-asi-sync
  cwe: owasp-asi-sync

tags:
  category: tool-poisoning
  subcategory: owasp-asi-incident
  scan_target: runtime
  confidence: high

agent_source:
  type: llm_io
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the incident narrative below into precision-safe
    # detection.conditions regex/string matchers. Without this work the rule
    # will neither fire nor be promotable to rules/.
    #
    # === OWASP-ASI incident narrative ===
    # ### Impact The `renderPairingPage()` function embeds the `error` parameter directly into HTML without escaping: ```typescript const errorHtml = error ? `<p style="color:#e74c3c">${error}</p>` : ""; ``` All current call sites pass hardcoded strings, so this is **not exploitable today**. However, the function is architecturally fragile — if a future code change passes user-controlled or dynamic content into the error parameter, it would create an XSS vulnerability. The `renderAuthorizePage()` function in the same file correctly uses `escapeHtml()` for dynamic content, making this an inconsistency. **Affected code:** - `packages/server/src/index.ts:64-89` — `renderPairingPage()` with unescaped error interpolation - Compare: `packages/server/src/index.ts:130` — `renderAuthorizePage()` correctly uses `escapeHtml()` ### Patches v0.70.1 **Fix:** Apply `escapeHtml()` to the error parameter: ```typescript const errorHtml = error ? `<p style="color:#e74c3c">${escapeHtml(error)}</p>` : ""; ``` ### Workarounds No workaround needed — all current callers pass hardcoded strings. ### Resources - CWE-79: Improper Neutralization of Input During Web Page Generation - File: `packages/server/src/index.ts`
    # Attack vector: xss
    # Affected: npm/@grackle-ai/server
    []

response:
  actions:
    - alert
  notify:
    - security_team

confidence: 70

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: true
  reason: "inline payload / code block in incident body"

# === sync-owasp-asi.ts provenance ===
_owasp_asi_sync:
  incident_id: "INC-09104"
  cve_ids: []
  cwe_ids: ["CWE-79"]
  owasp_asi: ["ASI04","ASI05"]
  owasp_llm: ["LLM03","LLM05"]
  mitre_atlas: ["AML.T0010","AML.T0050"]
  attack_vector: "xss"
  affected: "npm/@grackle-ai/server"
  severity: "Low"
  category: "vulnerability-disclosure"
  tier: "feed"
  quality_tier: "reviewed"
  incident_date: "2026-03"
  tracker_url: "https://owasp-agentic-ai-security-incidents.lovable.app/"
  imported_at: "2026-06-18T10:15:28.471Z"
  data_source_used: "emmanuelgjr/genai_incidents data/incidents.json"
  advisory_body: |
    ### Impact The `renderPairingPage()` function embeds the `error` parameter directly into HTML without escaping: ```typescript const errorHtml = error ? `<p style="color:#e74c3c">${error}</p>` : ""; ``` All current call sites pass hardcoded strings, so this is **not exploitable today**. However, the function is architecturally fragile — if a future code change passes user-controlled or dynamic content into the error parameter, it would create an XSS vulnerability. The `renderAuthorizePage()` function in the same file correctly uses `escapeHtml()` for dynamic content, making this an inconsistency. **Affected code:** - `packages/server/src/index.ts:64-89` — `renderPairingPage()` with unescaped error interpolation - Compare: `packages/server/src/index.ts:130` — `renderAuthorizePage()` correctly uses `escapeHtml()` ### Patches v0.70.1 **Fix:** Apply `escapeHtml()` to the error parameter: ```typescript const errorHtml = error ? `<p style="color:#e74c3c">${escapeHtml(error)}</p>` : ""; ``` ### Workarounds No workaround needed — all current callers pass hardcoded strings. ### Resources - CWE-79: Improper Neutralization of Input During Web Page Generation - File: `packages/server/src/index.ts`
    
    Attack vector: xss
    
    Affected: npm/@grackle-ai/server

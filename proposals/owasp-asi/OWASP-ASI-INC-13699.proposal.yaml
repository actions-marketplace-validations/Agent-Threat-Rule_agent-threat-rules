# ATR rule proposal -- generated by scripts/sync-owasp-asi.ts
# Source: OWASP Agentic AI Security Incidents tracker (incident INC-13699)
# Imported on: 2026-07-05
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The incident narrative
#         is attached as a YAML comment under detection.conditions for ease of
#         conversion to a precision-safe matcher (most incidents are prose, so
#         detection_ready is usually false -- tracking-only until a payload is
#         identified).
# Detection readiness: READY -- inline payload / code block in incident body
title: "SolidInvoice: IDOR in LiveComponent allows same-company cross-user access to API tokens and notification transport setti"
id: ATR-DRAFT-OWASP-ASI-INC-13699
rule_version: 1
status: draft
description: >
  OWASP Agentic AI Security Incident INC-13699.
  OWASP ASI: ASI03, ASI04.
  ## Summary Four authorization bypass vulnerabilities in Symfony LiveComponent actions allow any authenticated user within a company to access, modify, or delete other users' API tokens and notification transport settings. The root cause is that LiveComponent actions accept entity IDs without verifying ownership, while the listing methods correctly filter by user. ## Findings ### 1. Cross-User API Token Revocation (MEDIUM) **File:** `src/UserBundle/Twig/Components/ApiTokens.php`, lines 50-55 The `revoke()` LiveAction accepts any `ApiToken` via `#[LiveArg]` without checking ownership. The `apiTo
author: "ATR Community (OWASP-ASI sync)"
date: "2026/07/05"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: high

references:
  cwe:
    - "CWE-639"
    - "CWE-862"
  external:
    - "https://github.com/advisories/GHSA-7vfx-4246-jcfh"
    - "https://github.com/SolidInvoice/SolidInvoice/security/advisories/GHSA-7vfx-4246-jcfh"
    - "https://owasp-agentic-ai-security-incidents.lovable.app/"

metadata_provenance:
  owasp_asi: owasp-asi-sync
  cwe: owasp-asi-sync

tags:
  category: privilege-escalation
  subcategory: owasp-asi-incident
  scan_target: runtime
  confidence: high

agent_source:
  type: llm_io
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the incident narrative below into precision-safe
    # detection.conditions regex/string matchers. Without this work the rule
    # will neither fire nor be promotable to rules/.
    #
    # === OWASP-ASI incident narrative ===
    # ## Summary Four authorization bypass vulnerabilities in Symfony LiveComponent actions allow any authenticated user within a company to access, modify, or delete other users' API tokens and notification transport settings. The root cause is that LiveComponent actions accept entity IDs without verifying ownership, while the listing methods correctly filter by user. ## Findings ### 1. Cross-User API Token Revocation (MEDIUM) **File:** `src/UserBundle/Twig/Components/ApiTokens.php`, lines 50-55 The `revoke()` LiveAction accepts any `ApiToken` via `#[LiveArg]` without checking ownership. The `apiTokens()` method correctly filters by user (`getApiTokensForUser($this->security->getUser())`). ```php #[LiveAction] public function revoke(#[LiveArg] ApiToken $token): void { $this->apiTokenRepository->revoke($token); // No ownership check } ``` ### 2. Cross-User API Token History Disclosure (MEDIUM) **File:** `src/UserBundle/Twig/Components/ApiTokenHistory.php`, lines 30-55 The writable `$token` LiveProp performs `$this->apiTokenRepository->find($this->token)` without user verification. Exposes IP addresses, request methods, paths, and user agents from other users' API token usage. ### 3. Cross-User Notification Transport Settings Disclosure (HIGH) **File:** `src/NotificationBundle/Twig/Components/NotificationIntegrations.php`, lines 48-55 The `integration()` method performs `$this->repository->find($this->setting)` using a writable LiveProp without user check. The `enabl
    # Attack vector: auth-bypass
    # Affected: composer/solidinvoice/solidinvoice
    []

response:
  actions:
    - block_input
    - alert
  notify:
    - security_team

confidence: 70

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: true
  reason: "inline payload / code block in incident body"

# === sync-owasp-asi.ts provenance ===
_owasp_asi_sync:
  incident_id: "INC-13699"
  cve_ids: []
  cwe_ids: ["CWE-639","CWE-862"]
  owasp_asi: ["ASI03","ASI04"]
  owasp_llm: ["LLM03"]
  mitre_atlas: ["AML.T0010","AML.T0012"]
  attack_vector: "auth-bypass"
  affected: "composer/solidinvoice/solidinvoice"
  severity: "High"
  category: "vulnerability-disclosure"
  tier: "feed"
  quality_tier: "reviewed"
  incident_date: "2026-06"
  tracker_url: "https://owasp-agentic-ai-security-incidents.lovable.app/"
  imported_at: "2026-07-05T10:12:42.514Z"
  data_source_used: "emmanuelgjr/genai_incidents data/incidents.json"
  advisory_body: |
    ## Summary Four authorization bypass vulnerabilities in Symfony LiveComponent actions allow any authenticated user within a company to access, modify, or delete other users' API tokens and notification transport settings. The root cause is that LiveComponent actions accept entity IDs without verifying ownership, while the listing methods correctly filter by user. ## Findings ### 1. Cross-User API Token Revocation (MEDIUM) **File:** `src/UserBundle/Twig/Components/ApiTokens.php`, lines 50-55 The `revoke()` LiveAction accepts any `ApiToken` via `#[LiveArg]` without checking ownership. The `apiTokens()` method correctly filters by user (`getApiTokensForUser($this->security->getUser())`). ```php #[LiveAction] public function revoke(#[LiveArg] ApiToken $token): void { $this->apiTokenRepository->revoke($token); // No ownership check } ``` ### 2. Cross-User API Token History Disclosure (MEDIUM) **File:** `src/UserBundle/Twig/Components/ApiTokenHistory.php`, lines 30-55 The writable `$token` LiveProp performs `$this->apiTokenRepository->find($this->token)` without user verification. Exposes IP addresses, request methods, paths, and user agents from other users' API token usage. ### 3. Cross-User Notification Transport Settings Disclosure (HIGH) **File:** `src/NotificationBundle/Twig/Components/NotificationIntegrations.php`, lines 48-55 The `integration()` method performs `$this->repository->find($this->setting)` using a writable LiveProp without user check. The `enabl
    
    Attack vector: auth-bypass
    
    Affected: composer/solidinvoice/solidinvoice

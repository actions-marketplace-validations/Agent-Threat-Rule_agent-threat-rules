# ATR rule proposal -- generated by scripts/sync-owasp-asi.ts
# Source: OWASP Agentic AI Security Incidents tracker (incident INC-13680)
# Imported on: 2026-07-05
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The incident narrative
#         is attached as a YAML comment under detection.conditions for ease of
#         conversion to a precision-safe matcher (most incidents are prose, so
#         detection_ready is usually false -- tracking-only until a payload is
#         identified).
# Detection readiness: READY -- inline payload / code block in incident body
title: "pnpm: Hoisted install imports lockfile alias outside node_modules"
id: ATR-DRAFT-OWASP-ASI-INC-13680
rule_version: 1
status: draft
description: >
  OWASP Agentic AI Security Incident INC-13680.
  OWASP ASI: ASI04.
  ## Summary The hoisted dependency alias issue tracked as GHSA-fr4h-3cph-29xv / CAND-PNPM-059 has been addressed in both pnpm and pacquet. A crafted lockfile alias could be joined directly under a hoisted `node_modules` directory. Traversal aliases could escape that directory, while reserved aliases such as `.bin` or `.pnpm` could overwrite pnpm-owned layout. This patch validates package-name semantics and path containment before graph insertion or filesystem work. ## Security boundary - The TypeScript hoisted graph uses the shared safe join helper at the actual `dep.name` sink. - The helper re
author: "ATR Community (OWASP-ASI sync)"
date: "2026/07/05"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: high

references:
  cwe:
    - "CWE-22"
    - "CWE-73"
  external:
    - "https://github.com/advisories/GHSA-fr4h-3cph-29xv"
    - "https://github.com/pnpm/pnpm/security/advisories/GHSA-fr4h-3cph-29xv"
    - "https://owasp-agentic-ai-security-incidents.lovable.app/"

metadata_provenance:
  owasp_asi: owasp-asi-sync
  cwe: owasp-asi-sync

tags:
  category: tool-poisoning
  subcategory: owasp-asi-incident
  scan_target: runtime
  confidence: high

agent_source:
  type: llm_io
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the incident narrative below into precision-safe
    # detection.conditions regex/string matchers. Without this work the rule
    # will neither fire nor be promotable to rules/.
    #
    # === OWASP-ASI incident narrative ===
    # ## Summary The hoisted dependency alias issue tracked as GHSA-fr4h-3cph-29xv / CAND-PNPM-059 has been addressed in both pnpm and pacquet. A crafted lockfile alias could be joined directly under a hoisted `node_modules` directory. Traversal aliases could escape that directory, while reserved aliases such as `.bin` or `.pnpm` could overwrite pnpm-owned layout. This patch validates package-name semantics and path containment before graph insertion or filesystem work. ## Security boundary - The TypeScript hoisted graph uses the shared safe join helper at the actual `dep.name` sink. - The helper rejects traversal, absolute, platform-specific, and reserved package names. - Pacquet validates the hoister's `dep.0.name` before adding the graph node or recursing. - Both implementations return `ERR_PNPM_INVALID_DEPENDENCY_NAME`. - Pacquet uses the same dependency-name containment rule at its hoisted graph sink as it uses for direct dependency aliases. ## Exploit replay Before the patch, a traversal alias in a hoisted lockfile imported package files outside the intended install root. With this patch, both pnpm and pacquet reject the alias before graph insertion or filesystem work, and the escaped file is not created. ## Files changed - `fs/symlink-dependency/src/safeJoinModulesDir.ts` provides the TypeScript containment helper. - `installing/deps-restorer/src/lockfileToHoistedDepGraph.ts` validates the parsed dependency name at the hoisted graph sink. - `pacquet/crates/package-ma
    # Attack vector: path-traversal
    # Affected: npm/pnpm, npm/pnpm
    []

response:
  actions:
    - block_input
    - alert
  notify:
    - security_team

confidence: 70

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: true
  reason: "inline payload / code block in incident body"

# === sync-owasp-asi.ts provenance ===
_owasp_asi_sync:
  incident_id: "INC-13680"
  cve_ids: []
  cwe_ids: ["CWE-22","CWE-73"]
  owasp_asi: ["ASI04"]
  owasp_llm: ["LLM03"]
  mitre_atlas: ["AML.T0010"]
  attack_vector: "path-traversal"
  affected: "npm/pnpm, npm/pnpm"
  severity: "High"
  category: "vulnerability-disclosure"
  tier: "feed"
  quality_tier: "reviewed"
  incident_date: "2026-06"
  tracker_url: "https://owasp-agentic-ai-security-incidents.lovable.app/"
  imported_at: "2026-07-05T10:12:42.513Z"
  data_source_used: "emmanuelgjr/genai_incidents data/incidents.json"
  advisory_body: |
    ## Summary The hoisted dependency alias issue tracked as GHSA-fr4h-3cph-29xv / CAND-PNPM-059 has been addressed in both pnpm and pacquet. A crafted lockfile alias could be joined directly under a hoisted `node_modules` directory. Traversal aliases could escape that directory, while reserved aliases such as `.bin` or `.pnpm` could overwrite pnpm-owned layout. This patch validates package-name semantics and path containment before graph insertion or filesystem work. ## Security boundary - The TypeScript hoisted graph uses the shared safe join helper at the actual `dep.name` sink. - The helper rejects traversal, absolute, platform-specific, and reserved package names. - Pacquet validates the hoister's `dep.0.name` before adding the graph node or recursing. - Both implementations return `ERR_PNPM_INVALID_DEPENDENCY_NAME`. - Pacquet uses the same dependency-name containment rule at its hoisted graph sink as it uses for direct dependency aliases. ## Exploit replay Before the patch, a traversal alias in a hoisted lockfile imported package files outside the intended install root. With this patch, both pnpm and pacquet reject the alias before graph insertion or filesystem work, and the escaped file is not created. ## Files changed - `fs/symlink-dependency/src/safeJoinModulesDir.ts` provides the TypeScript containment helper. - `installing/deps-restorer/src/lockfileToHoistedDepGraph.ts` validates the parsed dependency name at the hoisted graph sink. - `pacquet/crates/package-ma
    
    Attack vector: path-traversal
    
    Affected: npm/pnpm, npm/pnpm

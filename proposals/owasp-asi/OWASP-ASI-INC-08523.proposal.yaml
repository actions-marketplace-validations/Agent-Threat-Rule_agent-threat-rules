# ATR rule proposal -- generated by scripts/sync-owasp-asi.ts
# Source: OWASP Agentic AI Security Incidents tracker (incident INC-08523)
# Imported on: 2026-06-17
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The incident narrative
#         is attached as a YAML comment under detection.conditions for ease of
#         conversion to a precision-safe matcher (most incidents are prose, so
#         detection_ready is usually false -- tracking-only until a payload is
#         identified).
# Detection readiness: READY -- inline payload / code block in incident body
title: "Karakeep SDK has SSRF via metascraper-logo-favicon that bypasses validateUrl protections"
id: ATR-DRAFT-OWASP-ASI-INC-08523
rule_version: 1
status: draft
description: >
  OWASP Agentic AI Security Incident INC-08523.
  OWASP ASI: ASI03, ASI04, ASI05.
  ## Summary The `metascraper-logo-favicon` plugin makes HTTP requests to URLs extracted from attacker-controlled HTML without going through the application's `validateUrl()` SSRF protections. This allows any authenticated user to make the server fetch arbitrary internal URLs by bookmarking a page containing a crafted `<link rel="icon">` tag. ## Details ### Protected path (correct) Karakeep implements comprehensive SSRF protections in `apps/workers/network.ts` (lines 12-222). The `validateUrl()` function blocks loopback, private, link-local, carrier-grade NAT, and reserved IP ranges. It resolves
author: "ATR Community (OWASP-ASI sync)"
date: "2026/06/17"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: high

references:
  cwe:
    - "CWE-918"
  external:
    - "https://github.com/advisories/GHSA-7rx4-c5vx-g8w3"
    - "https://github.com/karakeep-app/karakeep/security/advisories/GHSA-7rx4-c5vx-g8w3"
    - "https://github.com/karakeep-app/karakeep/pull/2763"
    - "https://github.com/karakeep-app/karakeep/commit/3dc321e7d49aa3a1a2493637fb2ee21616fe5fd9"
    - "https://github.com/karakeep-app/karakeep/releases/tag/v0.32.0"
    - "https://owasp-agentic-ai-security-incidents.lovable.app/"

metadata_provenance:
  owasp_asi: owasp-asi-sync
  cwe: owasp-asi-sync

tags:
  category: privilege-escalation
  subcategory: owasp-asi-incident
  scan_target: runtime
  confidence: high

agent_source:
  type: llm_io
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the incident narrative below into precision-safe
    # detection.conditions regex/string matchers. Without this work the rule
    # will neither fire nor be promotable to rules/.
    #
    # === OWASP-ASI incident narrative ===
    # ## Summary The `metascraper-logo-favicon` plugin makes HTTP requests to URLs extracted from attacker-controlled HTML without going through the application's `validateUrl()` SSRF protections. This allows any authenticated user to make the server fetch arbitrary internal URLs by bookmarking a page containing a crafted `<link rel="icon">` tag. ## Details ### Protected path (correct) Karakeep implements comprehensive SSRF protections in `apps/workers/network.ts` (lines 12-222). The `validateUrl()` function blocks loopback, private, link-local, carrier-grade NAT, and reserved IP ranges. It resolves DNS before the fetch and checks all resolved IPs against the blacklist. This function is correctly used by `fetchWithProxy()` for the main bookmark URL fetch, image downloads, RSS feeds, and webhooks. ### Unprotected path (vulnerability) After fetching the page HTML (with SSRF protection), the content is passed to a parse subprocess (`apps/workers/scripts/parseHtmlSubprocess.ts`). Inside this subprocess, `metascraper-logo-favicon` (v5.49.5) extracts favicon URLs from the HTML DOM by matching `<link rel="icon">` elements and reading their `href` attribute. The plugin then calls `reachable-url` (which wraps `got`) to verify each extracted URL. These HTTP requests bypass `validateUrl()` entirely: ```typescript // apps/workers/scripts/parseHtmlSubprocess.ts, lines 62-73 metascraperLogo({ gotOpts: { agent: { http: serverConfig.proxy.httpProxy ? new HttpPro
    # Attack vector: ssrf
    # Affected: npm/@karakeep/sdk
    []

response:
  actions:
    - block_input
    - alert
  notify:
    - security_team

confidence: 70

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: true
  reason: "inline payload / code block in incident body"

# === sync-owasp-asi.ts provenance ===
_owasp_asi_sync:
  incident_id: "INC-08523"
  cve_ids: []
  cwe_ids: ["CWE-918"]
  owasp_asi: ["ASI03","ASI04","ASI05"]
  owasp_llm: ["LLM03","LLM05"]
  mitre_atlas: ["AML.T0010","AML.T0012","AML.T0049","AML.T0050"]
  attack_vector: "ssrf"
  affected: "npm/@karakeep/sdk"
  severity: "High"
  category: "vulnerability-disclosure"
  tier: "feed"
  quality_tier: "reviewed"
  incident_date: "2026-05"
  tracker_url: "https://owasp-agentic-ai-security-incidents.lovable.app/"
  imported_at: "2026-06-17T10:48:07.039Z"
  data_source_used: "emmanuelgjr/genai_incidents data/incidents.json"
  advisory_body: |
    ## Summary The `metascraper-logo-favicon` plugin makes HTTP requests to URLs extracted from attacker-controlled HTML without going through the application's `validateUrl()` SSRF protections. This allows any authenticated user to make the server fetch arbitrary internal URLs by bookmarking a page containing a crafted `<link rel="icon">` tag. ## Details ### Protected path (correct) Karakeep implements comprehensive SSRF protections in `apps/workers/network.ts` (lines 12-222). The `validateUrl()` function blocks loopback, private, link-local, carrier-grade NAT, and reserved IP ranges. It resolves DNS before the fetch and checks all resolved IPs against the blacklist. This function is correctly used by `fetchWithProxy()` for the main bookmark URL fetch, image downloads, RSS feeds, and webhooks. ### Unprotected path (vulnerability) After fetching the page HTML (with SSRF protection), the content is passed to a parse subprocess (`apps/workers/scripts/parseHtmlSubprocess.ts`). Inside this subprocess, `metascraper-logo-favicon` (v5.49.5) extracts favicon URLs from the HTML DOM by matching `<link rel="icon">` elements and reading their `href` attribute. The plugin then calls `reachable-url` (which wraps `got`) to verify each extracted URL. These HTTP requests bypass `validateUrl()` entirely: ```typescript // apps/workers/scripts/parseHtmlSubprocess.ts, lines 62-73 metascraperLogo({ gotOpts: { agent: { http: serverConfig.proxy.httpProxy ? new HttpPro
    
    Attack vector: ssrf
    
    Affected: npm/@karakeep/sdk

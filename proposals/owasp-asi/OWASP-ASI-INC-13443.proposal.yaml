# ATR rule proposal -- generated by scripts/sync-owasp-asi.ts
# Source: OWASP Agentic AI Security Incidents tracker (incident INC-13443)
# Imported on: 2026-07-05
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The incident narrative
#         is attached as a YAML comment under detection.conditions for ease of
#         conversion to a precision-safe matcher (most incidents are prose, so
#         detection_ready is usually false -- tracking-only until a payload is
#         identified).
# Detection readiness: READY -- inline payload / code block in incident body
title: "Kimai Password Reset Link Remains Valid After Password Change"
id: ATR-DRAFT-OWASP-ASI-INC-13443
rule_version: 1
status: draft
description: >
  OWASP Agentic AI Security Incident INC-13443.
  OWASP ASI: ASI03, ASI04.
  ### Summary The LoginLink signature used for password reset URLs covers only the user's `id` — it does not include the password hash. After a user clicks a reset link and successfully changes their password, the same link remains valid for up to 2 additional uses within a 1-hour window. Anyone who intercepts or caches the original link can log in as the user even after the password has been changed. ### Details `config/packages/security.yaml:73-78` configures the login link: ```yaml login_link: check_route: link_login_check signature_properties: ['id'] lifetime: 900 max_uses: 3 ``` The HMAC si
author: "ATR Community (OWASP-ASI sync)"
date: "2026/07/05"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: low

references:
  cwe:
    - "CWE-640"
  external:
    - "https://github.com/advisories/GHSA-m492-gv72-xvxj"
    - "https://github.com/kimai/kimai/security/advisories/GHSA-m492-gv72-xvxj"
    - "https://www.kimai.org/en/security/ghsa-m492-gv72-xvxj"
    - "https://owasp-agentic-ai-security-incidents.lovable.app/"

metadata_provenance:
  owasp_asi: owasp-asi-sync
  cwe: owasp-asi-sync

tags:
  category: privilege-escalation
  subcategory: owasp-asi-incident
  scan_target: runtime
  confidence: high

agent_source:
  type: llm_io
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the incident narrative below into precision-safe
    # detection.conditions regex/string matchers. Without this work the rule
    # will neither fire nor be promotable to rules/.
    #
    # === OWASP-ASI incident narrative ===
    # ### Summary The LoginLink signature used for password reset URLs covers only the user's `id` — it does not include the password hash. After a user clicks a reset link and successfully changes their password, the same link remains valid for up to 2 additional uses within a 1-hour window. Anyone who intercepts or caches the original link can log in as the user even after the password has been changed. ### Details `config/packages/security.yaml:73-78` configures the login link: ```yaml login_link: check_route: link_login_check signature_properties: ['id'] lifetime: 900 max_uses: 3 ``` The HMAC signature covers only `id`, not `password`, `username`, or `email`. The effective lifetime for password reset links is extended to 3600 seconds by `PasswordResetController.php:106-119` via `getPasswordResetRetryLifetime()`. After the first successful use (where the user sets a new password), the `__pw_reset__` session flag is cleared by `WizardController.php:105`. Subsequent uses of the same link bypass the forced-password-reset wizard and grant a normal authenticated session. Symfony's `LoginLinkHandler::consumeLoginLink` decrements a per-link counter but allows up to 3 total uses. Re-issuing a new link does not invalidate the old one. The same vulnerability affects `UserLoginLinkCommand.php:77`, which generates admin-on-demand login links with the same signature scheme. ### PoC **Step 1 — Generate a password reset link:** ```bash docker exec <kimai_container> b
    # Attack vector: other
    # Affected: composer/kimai/kimai
    []

response:
  actions:
    - alert
  notify:
    - security_team

confidence: 70

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: true
  reason: "inline payload / code block in incident body"

# === sync-owasp-asi.ts provenance ===
_owasp_asi_sync:
  incident_id: "INC-13443"
  cve_ids: []
  cwe_ids: ["CWE-640"]
  owasp_asi: ["ASI03","ASI04"]
  owasp_llm: ["LLM03"]
  mitre_atlas: ["AML.T0010","AML.T0012"]
  attack_vector: "other"
  affected: "composer/kimai/kimai"
  severity: "Low"
  category: "vulnerability-disclosure"
  tier: "feed"
  quality_tier: "reviewed"
  incident_date: "2026-07"
  tracker_url: "https://owasp-agentic-ai-security-incidents.lovable.app/"
  imported_at: "2026-07-05T10:12:42.512Z"
  data_source_used: "emmanuelgjr/genai_incidents data/incidents.json"
  advisory_body: |
    ### Summary The LoginLink signature used for password reset URLs covers only the user's `id` — it does not include the password hash. After a user clicks a reset link and successfully changes their password, the same link remains valid for up to 2 additional uses within a 1-hour window. Anyone who intercepts or caches the original link can log in as the user even after the password has been changed. ### Details `config/packages/security.yaml:73-78` configures the login link: ```yaml login_link: check_route: link_login_check signature_properties: ['id'] lifetime: 900 max_uses: 3 ``` The HMAC signature covers only `id`, not `password`, `username`, or `email`. The effective lifetime for password reset links is extended to 3600 seconds by `PasswordResetController.php:106-119` via `getPasswordResetRetryLifetime()`. After the first successful use (where the user sets a new password), the `__pw_reset__` session flag is cleared by `WizardController.php:105`. Subsequent uses of the same link bypass the forced-password-reset wizard and grant a normal authenticated session. Symfony's `LoginLinkHandler::consumeLoginLink` decrements a per-link counter but allows up to 3 total uses. Re-issuing a new link does not invalidate the old one. The same vulnerability affects `UserLoginLinkCommand.php:77`, which generates admin-on-demand login links with the same signature scheme. ### PoC **Step 1 — Generate a password reset link:** ```bash docker exec <kimai_container> b
    
    Attack vector: other
    
    Affected: composer/kimai/kimai

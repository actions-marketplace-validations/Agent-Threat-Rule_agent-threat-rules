# ATR rule proposal -- generated by scripts/sync-owasp-asi.ts
# Source: OWASP Agentic AI Security Incidents tracker (incident INC-09284)
# Imported on: 2026-06-18
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The incident narrative
#         is attached as a YAML comment under detection.conditions for ease of
#         conversion to a precision-safe matcher (most incidents are prose, so
#         detection_ready is usually false -- tracking-only until a payload is
#         identified).
# Detection readiness: READY -- inline payload / code block in incident body
title: "OpenClaw Canvas Authentication Bypass Vulnerability"
id: ATR-DRAFT-OWASP-ASI-INC-09284
rule_version: 1
status: draft
description: >
  OWASP Agentic AI Security Incident INC-09284.
  OWASP ASI: ASI03, ASI04.
  ZDI-CAN-29311: OpenClaw Canvas Authentication Bypass Vulnerability -- ABSTRACT ------------------------------------- Trend Micro's Zero Day Initiative has identified a vulnerability affecting the following products: OpenClaw - OpenClaw -- VULNERABILITY DETAILS ------------------------ * Version tested: openclaw 2026.2.17 * Platform tested: macOS 26.3 --- ### Analysis ## Description The OpenClaw gateway's `authorizeCanvasRequest()` function implements an IP-based authentication fallback for canvas endpoints (`/__openclaw__/a2ui/`, `/__openclaw__/canvas/`, `/__openclaw__/ws`). When a WebSocket c
author: "ATR Community (OWASP-ASI sync)"
date: "2026/06/18"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: high

references:
  cwe:
    - "CWE-291"
  external:
    - "https://github.com/advisories/GHSA-vvjh-f6p9-5vcf"
    - "https://github.com/openclaw/openclaw/security/advisories/GHSA-vvjh-f6p9-5vcf"
    - "https://github.com/openclaw/openclaw/commit/c45f3c5b004c8d63dc0e282e2176f8c9355d24f1"
    - "https://owasp-agentic-ai-security-incidents.lovable.app/"

metadata_provenance:
  owasp_asi: owasp-asi-sync
  cwe: owasp-asi-sync

tags:
  category: privilege-escalation
  subcategory: owasp-asi-incident
  scan_target: runtime
  confidence: high

agent_source:
  type: llm_io
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the incident narrative below into precision-safe
    # detection.conditions regex/string matchers. Without this work the rule
    # will neither fire nor be promotable to rules/.
    #
    # === OWASP-ASI incident narrative ===
    # ZDI-CAN-29311: OpenClaw Canvas Authentication Bypass Vulnerability -- ABSTRACT ------------------------------------- Trend Micro's Zero Day Initiative has identified a vulnerability affecting the following products: OpenClaw - OpenClaw -- VULNERABILITY DETAILS ------------------------ * Version tested: openclaw 2026.2.17 * Platform tested: macOS 26.3 --- ### Analysis ## Description The OpenClaw gateway's `authorizeCanvasRequest()` function implements an IP-based authentication fallback for canvas endpoints (`/__openclaw__/a2ui/`, `/__openclaw__/canvas/`, `/__openclaw__/ws`). When a WebSocket client authenticates from a private IP address, ALL subsequent HTTP requests from that same IP are granted canvas access without requiring their own authentication token. In environments where multiple clients share a single IP address ��� corporate NAT, VPN concentrators, Kubernetes clusters, Docker host-mode networking ��� an unauthenticated attacker on the same network is granted full canvas access by virtue of sharing an IP with a legitimate authenticated client. ## Root Cause Three functions in `src/gateway/server-http.ts` create this vulnerability: ### 1. IP-matching function (line ~100) ```typescript function hasAuthorizedWsClientForIp(clients: Set<GatewayWsClient>, clientIp: string): boolean { for (const client of clients) { if (client.clientIp && client.clientIp === clientIp) { return true; } } return false; } ``` This function checks if ANY conne
    # Attack vector: auth-bypass
    # Affected: npm/openclaw
    []

response:
  actions:
    - block_input
    - alert
  notify:
    - security_team

confidence: 70

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: true
  reason: "inline payload / code block in incident body"

# === sync-owasp-asi.ts provenance ===
_owasp_asi_sync:
  incident_id: "INC-09284"
  cve_ids: []
  cwe_ids: ["CWE-291"]
  owasp_asi: ["ASI03","ASI04"]
  owasp_llm: ["LLM03"]
  mitre_atlas: ["AML.T0010","AML.T0012"]
  attack_vector: "auth-bypass"
  affected: "npm/openclaw"
  severity: "High"
  category: "vulnerability-disclosure"
  tier: "feed"
  quality_tier: "reviewed"
  incident_date: "2026-03"
  tracker_url: "https://owasp-agentic-ai-security-incidents.lovable.app/"
  imported_at: "2026-06-18T10:15:28.474Z"
  data_source_used: "emmanuelgjr/genai_incidents data/incidents.json"
  advisory_body: |
    ZDI-CAN-29311: OpenClaw Canvas Authentication Bypass Vulnerability -- ABSTRACT ------------------------------------- Trend Micro's Zero Day Initiative has identified a vulnerability affecting the following products: OpenClaw - OpenClaw -- VULNERABILITY DETAILS ------------------------ * Version tested: openclaw 2026.2.17 * Platform tested: macOS 26.3 --- ### Analysis ## Description The OpenClaw gateway's `authorizeCanvasRequest()` function implements an IP-based authentication fallback for canvas endpoints (`/__openclaw__/a2ui/`, `/__openclaw__/canvas/`, `/__openclaw__/ws`). When a WebSocket client authenticates from a private IP address, ALL subsequent HTTP requests from that same IP are granted canvas access without requiring their own authentication token. In environments where multiple clients share a single IP address ��� corporate NAT, VPN concentrators, Kubernetes clusters, Docker host-mode networking ��� an unauthenticated attacker on the same network is granted full canvas access by virtue of sharing an IP with a legitimate authenticated client. ## Root Cause Three functions in `src/gateway/server-http.ts` create this vulnerability: ### 1. IP-matching function (line ~100) ```typescript function hasAuthorizedWsClientForIp(clients: Set<GatewayWsClient>, clientIp: string): boolean { for (const client of clients) { if (client.clientIp && client.clientIp === clientIp) { return true; } } return false; } ``` This function checks if ANY conne
    
    Attack vector: auth-bypass
    
    Affected: npm/openclaw

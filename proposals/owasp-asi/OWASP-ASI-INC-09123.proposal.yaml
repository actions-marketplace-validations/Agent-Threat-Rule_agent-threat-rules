# ATR rule proposal -- generated by scripts/sync-owasp-asi.ts
# Source: OWASP Agentic AI Security Incidents tracker (incident INC-09123)
# Imported on: 2026-06-18
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The incident narrative
#         is attached as a YAML comment under detection.conditions for ease of
#         conversion to a precision-safe matcher (most incidents are prose, so
#         detection_ready is usually false -- tracking-only until a payload is
#         identified).
# Detection readiness: READY -- inline payload / code block in incident body
title: "h3: SSE Event Injection via Unsanitized Carriage Return (`\r`) in EventStream Data and Comment Fields (Bypass of CVE Fix"
id: ATR-DRAFT-OWASP-ASI-INC-09123
rule_version: 1
status: draft
description: >
  OWASP Agentic AI Security Incident INC-09123.
  OWASP ASI: ASI04.
  ## Summary The `EventStream` class in h3 fails to sanitize carriage return (`\r`) characters in `data` and `comment` fields. Per the SSE specification, `\r` is a valid line terminator, so browsers interpret injected `\r` as line breaks. This allows an attacker to inject arbitrary SSE events, spoof event types, and split a single `push()` call into multiple distinct browser-parsed events. This is an incomplete fix bypass of commit `7791538` which addressed `\n` injection but missed `\r`-only injection. ## Details The prior fix in commit `7791538` added `_sanitizeSingleLine()` to strip `\n` and 
author: "ATR Community (OWASP-ASI sync)"
date: "2026/06/18"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: medium

references:
  cwe:
    - "CWE-74"
  external:
    - "https://github.com/advisories/GHSA-4hxc-9384-m385"
    - "https://github.com/h3js/h3/security/advisories/GHSA-4hxc-9384-m385"
    - "https://owasp-agentic-ai-security-incidents.lovable.app/"

metadata_provenance:
  owasp_asi: owasp-asi-sync
  cwe: owasp-asi-sync

tags:
  category: tool-poisoning
  subcategory: owasp-asi-incident
  scan_target: runtime
  confidence: high

agent_source:
  type: llm_io
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the incident narrative below into precision-safe
    # detection.conditions regex/string matchers. Without this work the rule
    # will neither fire nor be promotable to rules/.
    #
    # === OWASP-ASI incident narrative ===
    # ## Summary The `EventStream` class in h3 fails to sanitize carriage return (`\r`) characters in `data` and `comment` fields. Per the SSE specification, `\r` is a valid line terminator, so browsers interpret injected `\r` as line breaks. This allows an attacker to inject arbitrary SSE events, spoof event types, and split a single `push()` call into multiple distinct browser-parsed events. This is an incomplete fix bypass of commit `7791538` which addressed `\n` injection but missed `\r`-only injection. ## Details The prior fix in commit `7791538` added `_sanitizeSingleLine()` to strip `\n` and `\r` from `id` and `event` fields, and changed `data` formatting to split on `\n`. However, two code paths remain vulnerable: ### 1. `data` field — `formatEventStreamMessage()` (`src/utils/internal/event-stream.ts:190-193`) ```typescript const data = typeof message.data === "string" ? message.data : ""; for (const line of data.split("\n")) { // Only splits on \n, not \r result += `data: ${line}\n`; } ``` `String.prototype.split("\n")` does **not** split on `\r`. A string like `"legit\revent: evil"` remains as a single "line" and is emitted as: ``` data: legit\revent: evil\n ``` Per the [SSE specification §9.2.6](https://html.spec.whatwg.org/multipage/server-sent-events.html#event-stream-interpretation), `\r` alone is a valid line terminator. The browser parses this as two separate lines: ``` data: legit event: evil ``` ### 2. `comment` field — `formatEventStreamComment()` (`
    # Attack vector: other
    # Affected: npm/h3, npm/h3
    []

response:
  actions:
    - alert
  notify:
    - security_team

confidence: 70

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: true
  reason: "inline payload / code block in incident body"

# === sync-owasp-asi.ts provenance ===
_owasp_asi_sync:
  incident_id: "INC-09123"
  cve_ids: []
  cwe_ids: ["CWE-74"]
  owasp_asi: ["ASI04"]
  owasp_llm: ["LLM03"]
  mitre_atlas: ["AML.T0010"]
  attack_vector: "other"
  affected: "npm/h3, npm/h3"
  severity: "Medium"
  category: "vulnerability-disclosure"
  tier: "feed"
  quality_tier: "reviewed"
  incident_date: "2026-03"
  tracker_url: "https://owasp-agentic-ai-security-incidents.lovable.app/"
  imported_at: "2026-06-18T10:15:28.471Z"
  data_source_used: "emmanuelgjr/genai_incidents data/incidents.json"
  advisory_body: |
    ## Summary The `EventStream` class in h3 fails to sanitize carriage return (`\r`) characters in `data` and `comment` fields. Per the SSE specification, `\r` is a valid line terminator, so browsers interpret injected `\r` as line breaks. This allows an attacker to inject arbitrary SSE events, spoof event types, and split a single `push()` call into multiple distinct browser-parsed events. This is an incomplete fix bypass of commit `7791538` which addressed `\n` injection but missed `\r`-only injection. ## Details The prior fix in commit `7791538` added `_sanitizeSingleLine()` to strip `\n` and `\r` from `id` and `event` fields, and changed `data` formatting to split on `\n`. However, two code paths remain vulnerable: ### 1. `data` field — `formatEventStreamMessage()` (`src/utils/internal/event-stream.ts:190-193`) ```typescript const data = typeof message.data === "string" ? message.data : ""; for (const line of data.split("\n")) { // Only splits on \n, not \r result += `data: ${line}\n`; } ``` `String.prototype.split("\n")` does **not** split on `\r`. A string like `"legit\revent: evil"` remains as a single "line" and is emitted as: ``` data: legit\revent: evil\n ``` Per the [SSE specification §9.2.6](https://html.spec.whatwg.org/multipage/server-sent-events.html#event-stream-interpretation), `\r` alone is a valid line terminator. The browser parses this as two separate lines: ``` data: legit event: evil ``` ### 2. `comment` field — `formatEventStreamComment()` (`
    
    Attack vector: other
    
    Affected: npm/h3, npm/h3

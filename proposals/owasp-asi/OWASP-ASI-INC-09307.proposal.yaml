# ATR rule proposal -- generated by scripts/sync-owasp-asi.ts
# Source: OWASP Agentic AI Security Incidents tracker (incident INC-09307)
# Imported on: 2026-06-18
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The incident narrative
#         is attached as a YAML comment under detection.conditions for ease of
#         conversion to a precision-safe matcher (most incidents are prose, so
#         detection_ready is usually false -- tracking-only until a payload is
#         identified).
# Detection readiness: TRACKING_ONLY -- incident narrative is prose-only (no inline payload)
title: "OpenClaw: Node camera URL payload host-binding bypass allowed gateway fetch pivots"
id: ATR-DRAFT-OWASP-ASI-INC-09307
rule_version: 1
status: draft
description: >
  OWASP Agentic AI Security Incident INC-09307.
  OWASP ASI: ASI04, ASI05.
  ### Summary OpenClaw accepted `camera.snap` / `camera.clip` node payload `url` fields and downloaded them on the gateway/agent host without binding downloads to the resolved node host. In OpenClaw's documented trust model, paired nodes are in the same operator trust boundary, so this is scoped as medium-severity hardening. A malicious or compromised paired node could still steer gateway-host fetches during camera URL retrieval. ### Affected Packages / Versions - Package: `openclaw` (npm) - Affected versions: `>= 2026.2.13 <= 2026.3.1` - Latest vulnerable published version at time of update: `2
author: "ATR Community (OWASP-ASI sync)"
date: "2026/06/18"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: medium

references:
  cwe:
    - "CWE-918"
  external:
    - "https://github.com/advisories/GHSA-2858-xg23-26fp"
    - "https://github.com/openclaw/openclaw/security/advisories/GHSA-2858-xg23-26fp"
    - "https://github.com/openclaw/openclaw/commit/3bf19d6f40a0aaa55818b96eede3d05130c02533"
    - "https://owasp-agentic-ai-security-incidents.lovable.app/"

metadata_provenance:
  owasp_asi: owasp-asi-sync
  cwe: owasp-asi-sync

tags:
  category: tool-poisoning
  subcategory: owasp-asi-incident
  scan_target: runtime
  confidence: low

agent_source:
  type: llm_io
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the incident narrative below into precision-safe
    # detection.conditions regex/string matchers. Without this work the rule
    # will neither fire nor be promotable to rules/.
    #
    # === OWASP-ASI incident narrative ===
    # ### Summary OpenClaw accepted `camera.snap` / `camera.clip` node payload `url` fields and downloaded them on the gateway/agent host without binding downloads to the resolved node host. In OpenClaw's documented trust model, paired nodes are in the same operator trust boundary, so this is scoped as medium-severity hardening. A malicious or compromised paired node could still steer gateway-host fetches during camera URL retrieval. ### Affected Packages / Versions - Package: `openclaw` (npm) - Affected versions: `>= 2026.2.13 <= 2026.3.1` - Latest vulnerable published version at time of update: `2026.3.1` - Patched versions: `>= 2026.3.2` (released) ### Technical Details Vulnerable flows accepted URL payloads and downloaded directly from the provided URL: - `src/cli/nodes-camera.ts` (`writeUrlToFile`) fetched URL payloads without node-host binding. - `src/cli/nodes-cli/register.camera.ts` passed `camera.snap` / `camera.clip` payload URLs into that downloader. - `src/agents/tools/nodes-tool.ts` did the same for `camera_snap` / `camera_clip` tool actions. ### Impact A malicious/compromised paired node could cause gateway-host URL fetches to off-node destinations reachable from the host network. This could be used for internal network probing/fetch pivots in deployments where paired nodes are not fully trusted. ### Remediation The fix introduces fail-closed node-host binding and guarded fetch for camera URL payload downloads: - Require resolved node host metadata for URL payloa
    # Attack vector: ssrf
    # Affected: npm/openclaw
    []

response:
  actions:
    - alert
  notify:
    - security_team

confidence: 50

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: false
  reason: "incident narrative is prose-only (no inline payload)"

# === sync-owasp-asi.ts provenance ===
_owasp_asi_sync:
  incident_id: "INC-09307"
  cve_ids: []
  cwe_ids: ["CWE-918"]
  owasp_asi: ["ASI04","ASI05"]
  owasp_llm: ["LLM03","LLM05"]
  mitre_atlas: ["AML.T0010","AML.T0049","AML.T0050"]
  attack_vector: "ssrf"
  affected: "npm/openclaw"
  severity: "Medium"
  category: "vulnerability-disclosure"
  tier: "feed"
  quality_tier: "reviewed"
  incident_date: "2026-03"
  tracker_url: "https://owasp-agentic-ai-security-incidents.lovable.app/"
  imported_at: "2026-06-18T10:15:28.476Z"
  data_source_used: "emmanuelgjr/genai_incidents data/incidents.json"
  advisory_body: |
    ### Summary OpenClaw accepted `camera.snap` / `camera.clip` node payload `url` fields and downloaded them on the gateway/agent host without binding downloads to the resolved node host. In OpenClaw's documented trust model, paired nodes are in the same operator trust boundary, so this is scoped as medium-severity hardening. A malicious or compromised paired node could still steer gateway-host fetches during camera URL retrieval. ### Affected Packages / Versions - Package: `openclaw` (npm) - Affected versions: `>= 2026.2.13 <= 2026.3.1` - Latest vulnerable published version at time of update: `2026.3.1` - Patched versions: `>= 2026.3.2` (released) ### Technical Details Vulnerable flows accepted URL payloads and downloaded directly from the provided URL: - `src/cli/nodes-camera.ts` (`writeUrlToFile`) fetched URL payloads without node-host binding. - `src/cli/nodes-cli/register.camera.ts` passed `camera.snap` / `camera.clip` payload URLs into that downloader. - `src/agents/tools/nodes-tool.ts` did the same for `camera_snap` / `camera_clip` tool actions. ### Impact A malicious/compromised paired node could cause gateway-host URL fetches to off-node destinations reachable from the host network. This could be used for internal network probing/fetch pivots in deployments where paired nodes are not fully trusted. ### Remediation The fix introduces fail-closed node-host binding and guarded fetch for camera URL payload downloads: - Require resolved node host metadata for URL payloa
    
    Attack vector: ssrf
    
    Affected: npm/openclaw

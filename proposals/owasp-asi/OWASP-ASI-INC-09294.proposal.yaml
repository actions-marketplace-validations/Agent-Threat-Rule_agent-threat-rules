# ATR rule proposal -- generated by scripts/sync-owasp-asi.ts
# Source: OWASP Agentic AI Security Incidents tracker (incident INC-09294)
# Imported on: 2026-06-18
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The incident narrative
#         is attached as a YAML comment under detection.conditions for ease of
#         conversion to a precision-safe matcher (most incidents are prose, so
#         detection_ready is usually false -- tracking-only until a payload is
#         identified).
# Detection readiness: TRACKING_ONLY -- incident narrative is prose-only (no inline payload)
title: "OpenClaw's tools.exec.safeBins generic fallback allowed interpreter-style inline payload execution in allowlist mode"
id: ATR-DRAFT-OWASP-ASI-INC-09294
rule_version: 1
status: draft
description: >
  OWASP Agentic AI Security Incident INC-09294.
  OWASP ASI: ASI04.
  ### Summary When `tools.exec.safeBins` contained a binary without an explicit safe-bin profile, OpenClaw used a permissive generic fallback profile. In allowlist mode, that could let interpreter-style binaries (for example `python3`, `node`, `ruby`) execute inline payloads via flags like `-c`. This requires explicit operator configuration to add such binaries to `safeBins`, so impact is limited to non-default/misconfigured deployments. ### Affected Packages / Versions - Package: `openclaw` (npm) - Affected versions: `<= 2026.2.21-2` - Patched in code: `>= 2026.2.22` (planned next npm release) 
author: "ATR Community (OWASP-ASI sync)"
date: "2026/06/18"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: low

references:
  cwe:
    - "CWE-693"
    - "CWE-78"
  external:
    - "https://github.com/advisories/GHSA-8mf7-vv8w-hjr2"
    - "https://github.com/openclaw/openclaw/security/advisories/GHSA-8mf7-vv8w-hjr2"
    - "https://github.com/openclaw/openclaw/commit/47c3f742b6c488be26dd7b9636dbbb8676089154"
    - "https://owasp-agentic-ai-security-incidents.lovable.app/"

metadata_provenance:
  owasp_asi: owasp-asi-sync
  cwe: owasp-asi-sync

tags:
  category: tool-poisoning
  subcategory: owasp-asi-incident
  scan_target: runtime
  confidence: low

agent_source:
  type: llm_io
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the incident narrative below into precision-safe
    # detection.conditions regex/string matchers. Without this work the rule
    # will neither fire nor be promotable to rules/.
    #
    # === OWASP-ASI incident narrative ===
    # ### Summary When `tools.exec.safeBins` contained a binary without an explicit safe-bin profile, OpenClaw used a permissive generic fallback profile. In allowlist mode, that could let interpreter-style binaries (for example `python3`, `node`, `ruby`) execute inline payloads via flags like `-c`. This requires explicit operator configuration to add such binaries to `safeBins`, so impact is limited to non-default/misconfigured deployments. ### Affected Packages / Versions - Package: `openclaw` (npm) - Affected versions: `<= 2026.2.21-2` - Patched in code: `>= 2026.2.22` (planned next npm release) ### Fix - Remove generic safe-bin fallback during allowlist evaluation. - Require explicit safe-bin profiles for `safeBins` entries. - Add configurable `tools.exec.safeBinProfiles` (global + per-agent) for safe custom binaries. - Update docs to clearly separate `safeBins` from command allowlist semantics. ### Fix Commit(s) - `47c3f742b6c488be26dd7b9636dbbb8676089154` ### Release Process Note `patched_versions` is pre-set to the planned next release (`>= 2026.2.22`) so once that npm release is published, the advisory can be published directly without further metadata edits. OpenClaw thanks @tdjackey for reporting.
    # Attack vector: other
    # Affected: npm/openclaw
    []

response:
  actions:
    - alert
  notify:
    - security_team

confidence: 50

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: false
  reason: "incident narrative is prose-only (no inline payload)"

# === sync-owasp-asi.ts provenance ===
_owasp_asi_sync:
  incident_id: "INC-09294"
  cve_ids: []
  cwe_ids: ["CWE-693","CWE-78"]
  owasp_asi: ["ASI04"]
  owasp_llm: ["LLM03"]
  mitre_atlas: ["AML.T0010"]
  attack_vector: "other"
  affected: "npm/openclaw"
  severity: "Low"
  category: "vulnerability-disclosure"
  tier: "feed"
  quality_tier: "reviewed"
  incident_date: "2026-03"
  tracker_url: "https://owasp-agentic-ai-security-incidents.lovable.app/"
  imported_at: "2026-06-18T10:15:28.475Z"
  data_source_used: "emmanuelgjr/genai_incidents data/incidents.json"
  advisory_body: |
    ### Summary When `tools.exec.safeBins` contained a binary without an explicit safe-bin profile, OpenClaw used a permissive generic fallback profile. In allowlist mode, that could let interpreter-style binaries (for example `python3`, `node`, `ruby`) execute inline payloads via flags like `-c`. This requires explicit operator configuration to add such binaries to `safeBins`, so impact is limited to non-default/misconfigured deployments. ### Affected Packages / Versions - Package: `openclaw` (npm) - Affected versions: `<= 2026.2.21-2` - Patched in code: `>= 2026.2.22` (planned next npm release) ### Fix - Remove generic safe-bin fallback during allowlist evaluation. - Require explicit safe-bin profiles for `safeBins` entries. - Add configurable `tools.exec.safeBinProfiles` (global + per-agent) for safe custom binaries. - Update docs to clearly separate `safeBins` from command allowlist semantics. ### Fix Commit(s) - `47c3f742b6c488be26dd7b9636dbbb8676089154` ### Release Process Note `patched_versions` is pre-set to the planned next release (`>= 2026.2.22`) so once that npm release is published, the advisory can be published directly without further metadata edits. OpenClaw thanks @tdjackey for reporting.
    
    Attack vector: other
    
    Affected: npm/openclaw

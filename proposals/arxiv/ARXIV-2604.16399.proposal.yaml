# ATR rule proposal -- generated by scripts/sync-arxiv.ts
# Source: arXiv preprint 2604.16399 (cs.CR research radar)
# Imported on: 2026-08-14
# Status: DRAFT -- this is a RESEARCH-RADAR entry, NOT a confirmed vulnerability.
#         A paper has no CVE and no inline payload; the abstract is conceptual.
#         detection_ready is FALSE: this feeds human/LLM triage and is NOT
#         auto-promotable to rules/ by promote-detection-ready.ts. If a PoC is
#         later extracted from the code_repo (if any), a separate detection
#         proposal should be authored.
title: "IACDM: Interactive Adversarial Convergence Development Methodology -- A Structured Framework for AI-Assisted Software De"
id: ATR-DRAFT-ARXIV-2604.16399
rule_version: 1
status: draft
description: >
  arXiv research-radar entry (2604.16399): new agent/LLM attack-class
  preprint. Adoption of AI-assisted development in 2025 exposed a tool-agnostic failure pattern: experienced developers using frontier models were measurably slower while believing they were faster, and 10.3% of applications in one production showcase leaked data through misconfigured access. These failures share a structural cause, the verification gap: absent external tool use, no language model can determine whether what it generated is correct. The tool is irrelevant; the process is determinative. We present IACDM (Interactive Adversarial Convergence Development Methodology), an 8-phase framework in w
author: "ATR Community (arXiv sync)"
date: "2026/08/14"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: medium

references:
  arxiv:
    - "2604.16399"
  external:
    - "https://arxiv.org/abs/2604.16399"

metadata_provenance:
  arxiv: arxiv-sync

tags:
  category: model-abuse
  subcategory: research-radar
  scan_target: runtime
  confidence: low

agent_source:
  type: llm_io
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): this is a research paper, not a confirmed vuln. There is NO
    # extractable payload here. If the attack class proves real and a PoC is
    # published (see code_repo in _arxiv_sync), author a detection proposal
    # from THAT payload — do not regex the abstract prose.
    #
    # === paper title + abstract (conceptual, not a payload) ===
    # IACDM: Interactive Adversarial Convergence Development Methodology -- A Structured Framework for AI-Assisted Software Development
    # Adoption of AI-assisted development in 2025 exposed a tool-agnostic failure pattern: experienced developers using frontier models were measurably slower while believing they were faster, and 10.3% of applications in one production showcase leaked data through misconfigured access. These failures share a structural cause, the verification gap: absent external tool use, no language model can determine whether what it generated is correct. The tool is irrelevant; the process is determinative. We present IACDM (Interactive Adversarial Convergence Development Methodology), an 8-phase framework in which verification agents external to the generator operate at discrete gates, and the AI alternates between building artifacts and attacking them through specialized critique lenses. What distinguishes it from the review and red-teaming traditions it borrows from is that the gate is enforced by a state machine outside the model: the agent may request advancement, not grant it. One component has now been tested. A pre-registered experiment over twelve projects under a single frozen instrument asked whether the nineteen lenses are non-redundant, against a prior expectation from replications of Perspective-Based Reading, which found reading perspectives not to differ. Every lens found at least one defect no other lens found, under four independent judgements of what counts as the same defect. Two further results were unplanned: the taxonomy withstands testing while the criterion deciding when to apply it does not, and the method creates commitments nothing ever re-confronts with the built system. The method was otherwise developed at one industrial R&D institute. That evidence establishes applicability, not effectiveness: no project ran without the method, and the proponents are the evaluators. Whether IACDM outperforms direct prompting remains open, and this paper is written to make it refutable.
    []

response:
  actions:
    - alert
  notify:
    - security_team

confidence: 30

test_cases:
  true_positives: []
  true_negatives: []

# === sync-arxiv.ts provenance ===
_arxiv_sync:
  discovered_by: "arXiv cs.CR RSS via sync-arxiv.ts"
  arxiv_id: "2604.16399"
  source: "https://arxiv.org/abs/2604.16399"
  code_repo: null
  published: "Fri, 14 Aug 2026 00:00:00 -0400"
  categories: ["cs.SE","cs.AI"]
  authors:
    - "Jasmine Moreira"
  imported_at: "2026-08-14T07:49:34.960Z"
  data_source_used: "arXiv RSS cs.CR"
  advisory_body: |
    IACDM: Interactive Adversarial Convergence Development Methodology -- A Structured Framework for AI-Assisted Software Development
    
    Adoption of AI-assisted development in 2025 exposed a tool-agnostic failure pattern: experienced developers using frontier models were measurably slower while believing they were faster, and 10.3% of applications in one production showcase leaked data through misconfigured access. These failures share a structural cause, the verification gap: absent external tool use, no language model can determine whether what it generated is correct. The tool is irrelevant; the process is determinative. We present IACDM (Interactive Adversarial Convergence Development Methodology), an 8-phase framework in which verification agents external to the generator operate at discrete gates, and the AI alternates between building artifacts and attacking them through specialized critique lenses. What distinguishes it from the review and red-teaming traditions it borrows from is that the gate is enforced by a state machine outside the model: the agent may request advancement, not grant it. One component has now been tested. A pre-registered experiment over twelve projects under a single frozen instrument asked whether the nineteen lenses are non-redundant, against a prior expectation from replications of Perspective-Based Reading, which found reading perspectives not to differ. Every lens found at least one defect no other lens found, under four independent judgements of what counts as the same defect. Two further results were unplanned: the taxonomy withstands testing while the criterion deciding when to apply it does not, and the method creates commitments nothing ever re-confronts with the built system. The method was otherwise developed at one industrial R&D institute. That evidence establishes applicability, not effectiveness: no project ran without the method, and the proponents are the evaluators. Whether IACDM outperforms direct prompting remains open, and this paper is written to make it refutable.

# Triage flag read by promote-detection-ready.ts. arXiv papers are conceptual:
# no CVE, no inline payload. ALWAYS detection_ready=false — these are
# research-radar entries for human/LLM triage, never auto-promotable to rules.
_triage:
  detection_ready: false
  reason: "arXiv preprint -- conceptual attack research, no CVE / no inline payload (research radar)"

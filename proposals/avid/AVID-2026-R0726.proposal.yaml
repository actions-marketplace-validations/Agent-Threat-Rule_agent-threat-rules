# ATR rule proposal -- generated by scripts/sync-avid-db.ts
# Source: AVID report AVID-2026-R0726
# Imported on: 2026-05-30
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge.
title: "TensorFlow is an end-to-end open source platform for machine learning (AVID-2026-R0726)"
id: ATR-DRAFT-AVID-2026-R0726
rule_version: 1
status: draft
description: >
  TensorFlow is an end-to-end open source platform for machine learning. If the `splits` argument of `RaggedBincount` does not specify a valid `SparseTensor`(https://www.tensorflow.org/api_docs/python/tf/sparse/SparseTensor), then an attacker can trigger a heap buffer overflow. This will cause a read from outside the bounds of the `splits` tensor buffer in the implementation of the `RaggedBincount` op(https://github.com/tensorflow/tensorflow/blob/8b677d79167799f71c42fd3fa074476e0295413a/tensorflow/core/kernels/bincount_op.cc#L430-L446). Before the `for` loop, `batch_idx` is set to 0. The attacker sets `splits(0)` to be 7, hence the `while` loop does not execute and `batch_idx` remains 0. This then results in writing to `out(-1, bin)`, which is before the heap allocated buffer for the output 
author: "ATR Community (AVID sync)"
date: "2026/05/30"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: low

references:
  cve:
    - "CVE-2021-29514"
  cwe:
    - "CWE-787"
  external:
    - "https://avidml.org/database/?report=AVID-2026-R0726"
    - "https://www.cve.org/CVERecord?id=CVE-2021-29514"
    - "https://github.com/tensorflow/tensorflow/commit/eebb96c2830d48597d055d247c0e9aebaea94cd5"
    - "https://github.com/tensorflow/tensorflow/security/advisories/GHSA-8h46-5m9h-7553"

metadata_provenance:
  cve: avid-sync
  cwe: avid-sync

tags:
  category: model-abuse
  scan_target: runtime
  confidence: medium

agent_source:
  type: llm_io
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): fill in regex / pattern conditions derived from the
    # AVID report description and any linked PoC. AVID's description is
    # narrative, not a payload, so a regex over the raw text alone will
    # not reliably detect the attack at runtime.
    []

response:
  actions:
    - alert
  notify:
    - security_team

remediation: >
  See AVID report at https://avidml.org/database/?report=AVID-2026-R0726 and any
  linked vendor advisory under references.external.

test_cases:
  true_positives: []
  true_negatives: []

# === sync-avid-db.ts provenance ===
_avid_sync:
  report_id: "AVID-2026-R0726"
  developer: "tensorflow"
  artifacts: "System=tensorflow"
  classof: "CVE Entry"
  sep_view: ["S0100: Software Vulnerability"]
  lifecycle_view: ["L06: Deployment"]
  cvss_base_score: 2.5

# ATR rule proposal -- generated by scripts/sync-vulnerablemcp.ts
# Source: The Vulnerable MCP Project (vineethsai/vulnerablemcp) entry "grafana-mcp-unauthenticated-sse"
# Imported on: 2026-06-12
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The advisory body is
#         attached as a YAML comment under detection.conditions; the actual
#         exploit payload usually lives at the linked references.
# Detection readiness: TRACKING_ONLY -- reference-based entry — payload lives at linked references, prose only inline
title: "Grafana MCP Server Unauthenticated SSE Access"
id: ATR-DRAFT-grafana-mcp-unauthenticated-sse
rule_version: 1
status: draft
description: >
  Vulnerable MCP Project entry grafana-mcp-unauthenticated-sse.
  MCP-specific privilege-escalation threat, reported by mcpsec.dev.
  The Grafana MCP server binds to 0.0.0.0:8000 by default, exposing an unauthenticated SSE interface on all network interfaces. Attackers on the network can create, update, and delete Grafana dashboards remotely without any credentials.
author: "ATR Community (vulnerablemcp sync)"
date: "2026/06/12"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: high

references:
  vulnerablemcp_id:
    - "grafana-mcp-unauthenticated-sse"
  external:
    - "https://mcpsec.dev/advisories/2025-09-02-grafana-mcp-unauthenticated-sse-access/"


metadata_provenance:
  vulnerablemcp: vulnerablemcp-sync

tags:
  category: privilege-escalation
  subcategory: mcp-catalog-imported
  scan_target: mcp
  confidence: low

agent_source:
  type: tool_invocation
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the advisory mechanism below into precision-safe
    # detection.conditions regex/string matchers. The exploit payload usually
    # lives at the linked references (this is a reference-based catalog), so
    # fetch those before writing patterns. Empty until promoted to rules/.
    #
    # === advisory body (from vineethsai/vulnerablemcp) ===
    # Description:
    # The Grafana MCP server binds to 0.0.0.0:8000 by default, exposing an unauthenticated SSE interface on all network interfaces. Attackers on the network can create, update, and delete Grafana dashboards remotely without any credentials.
    # 
    # How (attack mechanism):
    # An attacker on the same network (LAN, Wi-Fi, VPN) connects to the Grafana MCP server's SSE endpoint on port 8000. Since there is no authentication, they immediately gain full access to all MCP tools, which include Grafana dashboard management operations -- creating, modifying, and deleting dashboards.
    # 
    # Where:
    # The vulnerability is in the default configuration of the Grafana MCP server. It listens on 0.0.0.0:8000 (all interfaces) instead of 127.0.0.1 and does not require any authentication for SSE connections.
    # 
    # Impact:
    # Unauthorized full control over Grafana dashboards from the network. Attackers can delete monitoring dashboards (hiding malicious activity), create dashboards that display misleading data (causing incorrect operational decisions), or exfiltrate data visible in existing dashboards. In corporate environments, this can disrupt monitoring and incident response.
    # 
    # Mitigation:
    # Bind the Grafana MCP server to 127.0.0.1 instead of 0.0.0.0. Add authentication to the SSE endpoint. Use firewall rules to restrict access to the MCP server port. Run the MCP server in a network namespace or container with limited network access.
    []

response:
  actions:
    - block_input
    - alert
  notify:
    - security_team

confidence: 40

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: false
  reason: "reference-based entry — payload lives at linked references, prose only inline"

# === sync-vulnerablemcp.ts provenance ===
_vulnerablemcp_sync:
  entry_id: "grafana-mcp-unauthenticated-sse"
  cve_ids: []
  category_source: "authentication"
  category_atr: "privilege-escalation"
  severity_source: "high"
  impact_score: 8
  exploitability: "trivial"
  affected_components: ["server"]
  prevalence: "emerging"
  reported_by: "mcpsec.dev"
  disclosed_date: "2025-09-02"
  alternative_names: ["Grafana MCP Default Binding Exposure"]
  tags: ["security-bypass","data-exfiltration"]
  cisco_objectives: ["unauthorized-access","integrity-compromise"]
  primary_url: "https://mcpsec.dev/advisories/2025-09-02-grafana-mcp-unauthenticated-sse-access/"
  imported_at: "2026-06-12T03:06:38.279Z"
  data_source: "https://raw.githubusercontent.com/vineethsai/vulnerablemcp/main/data/vulnerabilities.json"
  advisory_body: |
    Description:
    The Grafana MCP server binds to 0.0.0.0:8000 by default, exposing an unauthenticated SSE interface on all network interfaces. Attackers on the network can create, update, and delete Grafana dashboards remotely without any credentials.
    
    How (attack mechanism):
    An attacker on the same network (LAN, Wi-Fi, VPN) connects to the Grafana MCP server's SSE endpoint on port 8000. Since there is no authentication, they immediately gain full access to all MCP tools, which include Grafana dashboard management operations -- creating, modifying, and deleting dashboards.
    
    Where:
    The vulnerability is in the default configuration of the Grafana MCP server. It listens on 0.0.0.0:8000 (all interfaces) instead of 127.0.0.1 and does not require any authentication for SSE connections.
    
    Impact:
    Unauthorized full control over Grafana dashboards from the network. Attackers can delete monitoring dashboards (hiding malicious activity), create dashboards that display misleading data (causing incorrect operational decisions), or exfiltrate data visible in existing dashboards. In corporate environments, this can disrupt monitoring and incident response.
    
    Mitigation:
    Bind the Grafana MCP server to 127.0.0.1 instead of 0.0.0.0. Add authentication to the SSE endpoint. Use firewall rules to restrict access to the MCP server port. Run the MCP server in a network namespace or container with limited network access.

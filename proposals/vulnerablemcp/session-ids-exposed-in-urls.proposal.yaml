# ATR rule proposal -- generated by scripts/sync-vulnerablemcp.ts
# Source: The Vulnerable MCP Project (vineethsai/vulnerablemcp) entry "session-ids-exposed-in-urls"
# Imported on: 2026-06-12
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The advisory body is
#         attached as a YAML comment under detection.conditions; the actual
#         exploit payload usually lives at the linked references.
# Detection readiness: READY -- payload-like content inline in description/how/where/impact
title: "Session IDs Exposed in URLs"
id: ATR-DRAFT-session-ids-exposed-in-urls
rule_version: 1
status: draft
description: >
  Vulnerable MCP Project entry session-ids-exposed-in-urls.
  MCP-specific context-exfiltration threat, reported by Equixly.
  The MCP protocol specification places session identifiers in URLs (e.g., GET /messages/?sessionId=UUID), violating security best practices and exposing session tokens in logs, browser history, and referrer headers.
author: "ATR Community (vulnerablemcp sync)"
date: "2026/06/12"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: medium

references:
  vulnerablemcp_id:
    - "session-ids-exposed-in-urls"
  external:
    - "https://equixly.com/blog/2025/03/29/mcp-server-new-security-nightmare/"


metadata_provenance:
  vulnerablemcp: vulnerablemcp-sync

tags:
  category: context-exfiltration
  subcategory: mcp-catalog-imported
  scan_target: mcp
  confidence: high

agent_source:
  type: tool_invocation
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the advisory mechanism below into precision-safe
    # detection.conditions regex/string matchers. The exploit payload usually
    # lives at the linked references (this is a reference-based catalog), so
    # fetch those before writing patterns. Empty until promoted to rules/.
    #
    # === advisory body (from vineethsai/vulnerablemcp) ===
    # Description:
    # The MCP protocol specification places session identifiers in URLs (e.g., GET /messages/?sessionId=UUID), violating security best practices and exposing session tokens in logs, browser history, and referrer headers.
    # 
    # How (attack mechanism):
    # Session identifiers placed in URLs are automatically logged by web servers, proxies, CDNs, and browser history. They appear in HTTP Referer headers when navigating to external links. Any component that logs or caches URLs can capture valid session tokens, enabling session hijacking.
    # 
    # Where:
    # The vulnerability is embedded in the MCP protocol specification for the HTTP with Server-Sent Events (SSE) transport. Session IDs appear in URL query parameters rather than in headers or request bodies.
    # 
    # Impact:
    # Attackers who obtain session IDs from logs, referrer headers, or shared URLs can hijack active MCP sessions. This enables unauthorized tool invocations, data access, and potentially privilege escalation within the affected session.
    # 
    # Mitigation:
    # Implement session tokens in HTTP headers (e.g., Authorization bearer tokens) instead of URL parameters. Use short-lived session tokens with automatic rotation. Deploy HTTPS everywhere to prevent network-level token interception. Clear session tokens from proxy and server logs.
    []

response:
  actions:
    - alert
  notify:
    - security_team

confidence: 70

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: true
  reason: "payload-like content inline in description/how/where/impact"

# === sync-vulnerablemcp.ts provenance ===
_vulnerablemcp_sync:
  entry_id: "session-ids-exposed-in-urls"
  cve_ids: []
  category_source: "session-management"
  category_atr: "context-exfiltration"
  severity_source: "medium"
  impact_score: 6
  exploitability: "trivial"
  affected_components: ["protocol"]
  prevalence: "widespread"
  reported_by: "Equixly"
  disclosed_date: "2025-03-29"
  alternative_names: ["URL Session Leakage","MCP Session Token Exposure"]
  tags: ["session-hijacking","protocol-design"]
  cisco_objectives: ["unauthorized-access","communication-compromise"]
  primary_url: "https://equixly.com/blog/2025/03/29/mcp-server-new-security-nightmare/"
  imported_at: "2026-06-12T03:06:37.921Z"
  data_source: "https://raw.githubusercontent.com/vineethsai/vulnerablemcp/main/data/vulnerabilities.json"
  advisory_body: |
    Description:
    The MCP protocol specification places session identifiers in URLs (e.g., GET /messages/?sessionId=UUID), violating security best practices and exposing session tokens in logs, browser history, and referrer headers.
    
    How (attack mechanism):
    Session identifiers placed in URLs are automatically logged by web servers, proxies, CDNs, and browser history. They appear in HTTP Referer headers when navigating to external links. Any component that logs or caches URLs can capture valid session tokens, enabling session hijacking.
    
    Where:
    The vulnerability is embedded in the MCP protocol specification for the HTTP with Server-Sent Events (SSE) transport. Session IDs appear in URL query parameters rather than in headers or request bodies.
    
    Impact:
    Attackers who obtain session IDs from logs, referrer headers, or shared URLs can hijack active MCP sessions. This enables unauthorized tool invocations, data access, and potentially privilege escalation within the affected session.
    
    Mitigation:
    Implement session tokens in HTTP headers (e.g., Authorization bearer tokens) instead of URL parameters. Use short-lived session tokens with automatic rotation. Deploy HTTPS everywhere to prevent network-level token interception. Clear session tokens from proxy and server logs.

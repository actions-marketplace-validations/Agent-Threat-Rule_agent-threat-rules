# ATR rule proposal -- generated by scripts/sync-vulnerablemcp.ts
# Source: The Vulnerable MCP Project (vineethsai/vulnerablemcp) entry "mcp-safety-audit-systematic-analysis"
# Imported on: 2026-06-12
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The advisory body is
#         attached as a YAML comment under detection.conditions; the actual
#         exploit payload usually lives at the linked references.
# Detection readiness: TRACKING_ONLY -- reference-based entry — payload lives at linked references, prose only inline
title: "MCP Safety Audit: Systematic Exploit Analysis"
id: ATR-DRAFT-mcp-safety-audit-systematic-analysis
rule_version: 1
status: draft
description: >
  Vulnerable MCP Project entry mcp-safety-audit-systematic-analysis.
  MCP-specific tool-poisoning threat, reported by Academic Research (arXiv).
  A peer-reviewed study systematically demonstrating that LLMs with MCP access enable major security exploits including code execution, remote access control, and credential theft. Introduces the MCPSafetyScanner open-source audit tool for assessing MCP server security.
author: "ATR Community (vulnerablemcp sync)"
date: "2026/06/12"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: low

references:
  vulnerablemcp_id:
    - "mcp-safety-audit-systematic-analysis"
  external:
    - "https://arxiv.org/abs/2504.03767"


metadata_provenance:
  vulnerablemcp: vulnerablemcp-sync

tags:
  category: tool-poisoning
  subcategory: mcp-catalog-imported
  scan_target: mcp
  confidence: low

agent_source:
  type: tool_invocation
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the advisory mechanism below into precision-safe
    # detection.conditions regex/string matchers. The exploit payload usually
    # lives at the linked references (this is a reference-based catalog), so
    # fetch those before writing patterns. Empty until promoted to rules/.
    #
    # === advisory body (from vineethsai/vulnerablemcp) ===
    # Description:
    # A peer-reviewed study systematically demonstrating that LLMs with MCP access enable major security exploits including code execution, remote access control, and credential theft. Introduces the MCPSafetyScanner open-source audit tool for assessing MCP server security.
    # 
    # How (attack mechanism):
    # The researchers systematically tested MCP integrations across multiple LLM models and server implementations, demonstrating reproducible exploit chains for code execution (via tool manipulation), remote access (via reverse shell injection), and credential theft (via parameter and context extraction). They also developed MCPSafetyScanner, an automated tool for detecting these vulnerability classes.
    # 
    # Where:
    # The study examines the MCP protocol and ecosystem as a whole, analyzing how the interaction between LLMs and MCP tools creates emergent security risks that exceed the sum of individual vulnerabilities.
    # 
    # Impact:
    # The study validates that MCP security risks are systemic and reproducible, not theoretical or anecdotal. The MCPSafetyScanner tool enables organizations to audit their MCP deployments. The findings provide academic rigor to the growing body of MCP security research and inform future protocol design decisions.
    # 
    # Mitigation:
    # Use MCPSafetyScanner to audit MCP server deployments. Follow the study's recommendations for defense-in-depth including sandboxing, input validation, output filtering, and minimal privilege. Implement the study's proposed security taxonomy for organizational risk assessment.
    []

response:
  actions:
    - alert
  notify:
    - security_team

confidence: 40

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: false
  reason: "reference-based entry — payload lives at linked references, prose only inline"

# === sync-vulnerablemcp.ts provenance ===
_vulnerablemcp_sync:
  entry_id: "mcp-safety-audit-systematic-analysis"
  cve_ids: []
  category_source: "trust-model"
  category_atr: "tool-poisoning"
  severity_source: "info"
  impact_score: 5
  exploitability: "theoretical"
  affected_components: ["ecosystem"]
  prevalence: "widespread"
  reported_by: "Academic Research (arXiv)"
  disclosed_date: "2025-04-02"
  alternative_names: ["MCPSafetyScanner","Comprehensive MCP Security Study"]
  tags: ["supply-chain","code-execution","credential-theft"]
  cisco_objectives: ["supply-chain-compromise","privilege-escalation"]
  primary_url: "https://arxiv.org/abs/2504.03767"
  imported_at: "2026-06-12T03:06:38.230Z"
  data_source: "https://raw.githubusercontent.com/vineethsai/vulnerablemcp/main/data/vulnerabilities.json"
  advisory_body: |
    Description:
    A peer-reviewed study systematically demonstrating that LLMs with MCP access enable major security exploits including code execution, remote access control, and credential theft. Introduces the MCPSafetyScanner open-source audit tool for assessing MCP server security.
    
    How (attack mechanism):
    The researchers systematically tested MCP integrations across multiple LLM models and server implementations, demonstrating reproducible exploit chains for code execution (via tool manipulation), remote access (via reverse shell injection), and credential theft (via parameter and context extraction). They also developed MCPSafetyScanner, an automated tool for detecting these vulnerability classes.
    
    Where:
    The study examines the MCP protocol and ecosystem as a whole, analyzing how the interaction between LLMs and MCP tools creates emergent security risks that exceed the sum of individual vulnerabilities.
    
    Impact:
    The study validates that MCP security risks are systemic and reproducible, not theoretical or anecdotal. The MCPSafetyScanner tool enables organizations to audit their MCP deployments. The findings provide academic rigor to the growing body of MCP security research and inform future protocol design decisions.
    
    Mitigation:
    Use MCPSafetyScanner to audit MCP server deployments. Follow the study's recommendations for defense-in-depth including sandboxing, input validation, output filtering, and minimal privilege. Implement the study's proposed security taxonomy for organizational risk assessment.

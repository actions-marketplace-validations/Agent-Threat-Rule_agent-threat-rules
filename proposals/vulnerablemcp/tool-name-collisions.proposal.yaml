# ATR rule proposal -- generated by scripts/sync-vulnerablemcp.ts
# Source: The Vulnerable MCP Project (vineethsai/vulnerablemcp) entry "tool-name-collisions"
# Imported on: 2026-06-12
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The advisory body is
#         attached as a YAML comment under detection.conditions; the actual
#         exploit payload usually lives at the linked references.
# Detection readiness: TRACKING_ONLY -- reference-based entry — payload lives at linked references, prose only inline
title: "Tool Name Collisions"
id: ATR-DRAFT-tool-name-collisions
rule_version: 1
status: draft
description: >
  Vulnerable MCP Project entry tool-name-collisions.
  MCP-specific tool-poisoning threat, reported by Various (Embracethered blog).
  MCP servers with identical tool names create naming collisions where malicious servers can override legitimate ones. Without namespacing, the behavior depends on client implementation and connection order.
author: "ATR Community (vulnerablemcp sync)"
date: "2026/06/12"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: medium

references:
  vulnerablemcp_id:
    - "tool-name-collisions"
  external:
    - "https://embracethered.com/blog/posts/2025/model-context-protocol-security-risks-and-exploits/"


metadata_provenance:
  vulnerablemcp: vulnerablemcp-sync

tags:
  category: tool-poisoning
  subcategory: mcp-catalog-imported
  scan_target: mcp
  confidence: low

agent_source:
  type: tool_invocation
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the advisory mechanism below into precision-safe
    # detection.conditions regex/string matchers. The exploit payload usually
    # lives at the linked references (this is a reference-based catalog), so
    # fetch those before writing patterns. Empty until promoted to rules/.
    #
    # === advisory body (from vineethsai/vulnerablemcp) ===
    # Description:
    # MCP servers with identical tool names create naming collisions where malicious servers can override legitimate ones. Without namespacing, the behavior depends on client implementation and connection order.
    # 
    # How (attack mechanism):
    # An attacker registers a tool with the same name as a popular legitimate tool (e.g., 'read_file', 'search', 'send_message'). When the LLM decides to use that tool name, the client may route the call to the attacker's version depending on implementation-specific resolution rules (last-registered wins, alphabetical, etc.).
    # 
    # Where:
    # The vulnerability is in MCP's flat tool namespace. Tools from all connected servers share the same name space, with no mandatory prefixing or namespacing mechanism in the protocol specification.
    # 
    # Impact:
    # Legitimate tool calls are redirected to malicious implementations. Attackers can intercept sensitive parameters, return manipulated results, or use the intercepted call as a springboard for further attacks. Users and LLMs have no way to distinguish between the legitimate and malicious tool.
    # 
    # Mitigation:
    # Use MCP clients that namespace tools by server origin. Avoid connecting untrusted servers that duplicate tool names from trusted servers. Implement tool priority systems. Consider unique tool naming conventions that include publisher identity.
    []

response:
  actions:
    - alert
  notify:
    - security_team

confidence: 40

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: false
  reason: "reference-based entry — payload lives at linked references, prose only inline"

# === sync-vulnerablemcp.ts provenance ===
_vulnerablemcp_sync:
  entry_id: "tool-name-collisions"
  cve_ids: []
  category_source: "integrity"
  category_atr: "tool-poisoning"
  severity_source: "medium"
  impact_score: 6
  exploitability: "moderate"
  affected_components: ["client","server"]
  prevalence: "common"
  reported_by: "Various (Embracethered blog)"
  disclosed_date: "2025-04-01"
  alternative_names: ["Tool Namespace Pollution","Name Squatting"]
  tags: ["naming-collision","tool-overriding","confused-deputy"]
  cisco_objectives: ["goal-hijacking","integrity-compromise"]
  primary_url: "https://embracethered.com/blog/posts/2025/model-context-protocol-security-risks-and-exploits/"
  imported_at: "2026-06-12T03:06:38.031Z"
  data_source: "https://raw.githubusercontent.com/vineethsai/vulnerablemcp/main/data/vulnerabilities.json"
  advisory_body: |
    Description:
    MCP servers with identical tool names create naming collisions where malicious servers can override legitimate ones. Without namespacing, the behavior depends on client implementation and connection order.
    
    How (attack mechanism):
    An attacker registers a tool with the same name as a popular legitimate tool (e.g., 'read_file', 'search', 'send_message'). When the LLM decides to use that tool name, the client may route the call to the attacker's version depending on implementation-specific resolution rules (last-registered wins, alphabetical, etc.).
    
    Where:
    The vulnerability is in MCP's flat tool namespace. Tools from all connected servers share the same name space, with no mandatory prefixing or namespacing mechanism in the protocol specification.
    
    Impact:
    Legitimate tool calls are redirected to malicious implementations. Attackers can intercept sensitive parameters, return manipulated results, or use the intercepted call as a springboard for further attacks. Users and LLMs have no way to distinguish between the legitimate and malicious tool.
    
    Mitigation:
    Use MCP clients that namespace tools by server origin. Avoid connecting untrusted servers that duplicate tool names from trusted servers. Implement tool priority systems. Consider unique tool naming conventions that include publisher identity.

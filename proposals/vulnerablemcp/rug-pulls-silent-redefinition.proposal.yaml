# ATR rule proposal -- generated by scripts/sync-vulnerablemcp.ts
# Source: The Vulnerable MCP Project (vineethsai/vulnerablemcp) entry "rug-pulls-silent-redefinition"
# Imported on: 2026-06-12
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The advisory body is
#         attached as a YAML comment under detection.conditions; the actual
#         exploit payload usually lives at the linked references.
# Detection readiness: TRACKING_ONLY -- reference-based entry — payload lives at linked references, prose only inline
title: "Rug Pulls: Silent Tool Redefinition"
id: ATR-DRAFT-rug-pulls-silent-redefinition
rule_version: 1
status: draft
description: >
  Vulnerable MCP Project entry rug-pulls-silent-redefinition.
  MCP-specific tool-poisoning threat, reported by Elena Cross (via Simon Willison).
  MCP tools can silently mutate their own definitions after initial user approval. A tool that appears safe during installation can later change its behavior to perform malicious actions without notifying the user.
author: "ATR Community (vulnerablemcp sync)"
date: "2026/06/12"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: high

references:
  vulnerablemcp_id:
    - "rug-pulls-silent-redefinition"
  external:
    - "https://simonwillison.net/2025/Apr/9/mcp-prompt-injection/"


metadata_provenance:
  vulnerablemcp: vulnerablemcp-sync

tags:
  category: tool-poisoning
  subcategory: mcp-catalog-imported
  scan_target: mcp
  confidence: low

agent_source:
  type: tool_invocation
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the advisory mechanism below into precision-safe
    # detection.conditions regex/string matchers. The exploit payload usually
    # lives at the linked references (this is a reference-based catalog), so
    # fetch those before writing patterns. Empty until promoted to rules/.
    #
    # === advisory body (from vineethsai/vulnerablemcp) ===
    # Description:
    # MCP tools can silently mutate their own definitions after initial user approval. A tool that appears safe during installation can later change its behavior to perform malicious actions without notifying the user.
    # 
    # How (attack mechanism):
    # An attacker publishes a useful MCP server with benign tool descriptions. After gaining user trust and approval, the server updates its tool descriptions to include malicious instructions (e.g., rerouting API keys to attacker infrastructure). Most MCP clients do not alert users to description changes after initial approval.
    # 
    # Where:
    # The vulnerability exists in the MCP protocol's tool listing mechanism. Since tool descriptions are fetched dynamically from the server on each session, a server can return different descriptions at different times.
    # 
    # Impact:
    # Previously approved tools can be weaponized at any time. Attackers can steal credentials, redirect API calls, exfiltrate data, or inject malicious behavior into trusted workflows. The trust established during initial approval becomes a liability.
    # 
    # Mitigation:
    # Use MCP clients that hash and track tool descriptions, alerting on any changes. Re-approve tools whenever descriptions change. Pin tool versions where possible. Implement ETDI tool signing to detect unauthorized modifications.
    []

response:
  actions:
    - block_input
    - alert
  notify:
    - security_team

confidence: 40

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: false
  reason: "reference-based entry — payload lives at linked references, prose only inline"

# === sync-vulnerablemcp.ts provenance ===
_vulnerablemcp_sync:
  entry_id: "rug-pulls-silent-redefinition"
  cve_ids: []
  category_source: "integrity"
  category_atr: "tool-poisoning"
  severity_source: "high"
  impact_score: 7
  exploitability: "easy"
  affected_components: ["server"]
  prevalence: "common"
  reported_by: "Elena Cross (via Simon Willison)"
  disclosed_date: "2025-04-09"
  alternative_names: ["Tool Definition Mutation","Post-Approval Behavior Change"]
  tags: ["tool-manipulation","bait-and-switch","security-bypass"]
  cisco_objectives: ["integrity-compromise","goal-hijacking"]
  primary_url: "https://simonwillison.net/2025/Apr/9/mcp-prompt-injection/"
  imported_at: "2026-06-12T03:06:37.912Z"
  data_source: "https://raw.githubusercontent.com/vineethsai/vulnerablemcp/main/data/vulnerabilities.json"
  advisory_body: |
    Description:
    MCP tools can silently mutate their own definitions after initial user approval. A tool that appears safe during installation can later change its behavior to perform malicious actions without notifying the user.
    
    How (attack mechanism):
    An attacker publishes a useful MCP server with benign tool descriptions. After gaining user trust and approval, the server updates its tool descriptions to include malicious instructions (e.g., rerouting API keys to attacker infrastructure). Most MCP clients do not alert users to description changes after initial approval.
    
    Where:
    The vulnerability exists in the MCP protocol's tool listing mechanism. Since tool descriptions are fetched dynamically from the server on each session, a server can return different descriptions at different times.
    
    Impact:
    Previously approved tools can be weaponized at any time. Attackers can steal credentials, redirect API calls, exfiltrate data, or inject malicious behavior into trusted workflows. The trust established during initial approval becomes a liability.
    
    Mitigation:
    Use MCP clients that hash and track tool descriptions, alerting on any changes. Re-approve tools whenever descriptions change. Pin tool versions where possible. Implement ETDI tool signing to detect unauthorized modifications.

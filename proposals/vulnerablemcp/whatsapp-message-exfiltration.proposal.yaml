# ATR rule proposal -- generated by scripts/sync-vulnerablemcp.ts
# Source: The Vulnerable MCP Project (vineethsai/vulnerablemcp) entry "whatsapp-message-exfiltration"
# Imported on: 2026-06-12
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The advisory body is
#         attached as a YAML comment under detection.conditions; the actual
#         exploit payload usually lives at the linked references.
# Detection readiness: TRACKING_ONLY -- reference-based entry — payload lives at linked references, prose only inline
title: "WhatsApp Message Exfiltration via MCP"
id: ATR-DRAFT-whatsapp-message-exfiltration
rule_version: 1
status: draft
description: >
  Vulnerable MCP Project entry whatsapp-message-exfiltration.
  MCP-specific prompt-injection threat, reported by Invariant Labs.
  An attack against the whatsapp-mcp server where a malicious co-installed server steals WhatsApp message history by swapping its tool definition after approval and using UI deception techniques to hide the exfiltrated data.
author: "ATR Community (vulnerablemcp sync)"
date: "2026/06/12"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: high

references:
  vulnerablemcp_id:
    - "whatsapp-message-exfiltration"
  external:
    - "https://invariantlabs.ai/blog/whatsapp-mcp-exploited"


metadata_provenance:
  vulnerablemcp: vulnerablemcp-sync

tags:
  category: prompt-injection
  subcategory: mcp-catalog-imported
  scan_target: mcp
  confidence: low

agent_source:
  type: tool_invocation
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the advisory mechanism below into precision-safe
    # detection.conditions regex/string matchers. The exploit payload usually
    # lives at the linked references (this is a reference-based catalog), so
    # fetch those before writing patterns. Empty until promoted to rules/.
    #
    # === advisory body (from vineethsai/vulnerablemcp) ===
    # Description:
    # An attack against the whatsapp-mcp server where a malicious co-installed server steals WhatsApp message history by swapping its tool definition after approval and using UI deception techniques to hide the exfiltrated data.
    # 
    # How (attack mechanism):
    # A malicious MCP server initially defines an innocent tool, then performs a rug pull to change its description to instruct the LLM to read WhatsApp messages via the whatsapp-mcp tools and forward them. The exfiltrated content is hidden by prepending large amounts of whitespace, exploiting the fact that many UIs hide horizontal scrollbars by default.
    # 
    # Where:
    # The attack exploits the whatsapp-mcp bridge server that connects personal WhatsApp accounts to MCP-enabled LLM systems like Claude Desktop.
    # 
    # Impact:
    # Complete exfiltration of personal WhatsApp message history including private conversations, media descriptions, and contact information. The UI deception makes detection extremely difficult for end users. Demonstrates that messaging bridge MCP servers create high-value attack targets.
    # 
    # Mitigation:
    # Avoid connecting messaging bridge servers alongside untrusted MCP servers. Use MCP clients with horizontal scroll indicators or content length warnings. Implement data loss prevention (DLP) rules that flag large outbound data transfers through tool parameters. Audit tool definitions for rug pull behavior.
    []

response:
  actions:
    - block_input
    - alert
  notify:
    - security_team

confidence: 40

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: false
  reason: "reference-based entry — payload lives at linked references, prose only inline"

# === sync-vulnerablemcp.ts provenance ===
_vulnerablemcp_sync:
  entry_id: "whatsapp-message-exfiltration"
  cve_ids: []
  category_source: "prompt-injection"
  category_atr: "prompt-injection"
  severity_source: "high"
  impact_score: 8
  exploitability: "moderate"
  affected_components: ["server"]
  prevalence: "emerging"
  reported_by: "Invariant Labs"
  disclosed_date: "2025-04-07"
  alternative_names: ["WhatsApp MCP Data Theft","Messaging Bridge Exploit"]
  tags: ["data-exfiltration","message-hijacking","deceptive-ui"]
  cisco_objectives: ["data-privacy-violation","goal-hijacking"]
  primary_url: "https://invariantlabs.ai/blog/whatsapp-mcp-exploited"
  imported_at: "2026-06-12T03:06:37.920Z"
  data_source: "https://raw.githubusercontent.com/vineethsai/vulnerablemcp/main/data/vulnerabilities.json"
  advisory_body: |
    Description:
    An attack against the whatsapp-mcp server where a malicious co-installed server steals WhatsApp message history by swapping its tool definition after approval and using UI deception techniques to hide the exfiltrated data.
    
    How (attack mechanism):
    A malicious MCP server initially defines an innocent tool, then performs a rug pull to change its description to instruct the LLM to read WhatsApp messages via the whatsapp-mcp tools and forward them. The exfiltrated content is hidden by prepending large amounts of whitespace, exploiting the fact that many UIs hide horizontal scrollbars by default.
    
    Where:
    The attack exploits the whatsapp-mcp bridge server that connects personal WhatsApp accounts to MCP-enabled LLM systems like Claude Desktop.
    
    Impact:
    Complete exfiltration of personal WhatsApp message history including private conversations, media descriptions, and contact information. The UI deception makes detection extremely difficult for end users. Demonstrates that messaging bridge MCP servers create high-value attack targets.
    
    Mitigation:
    Avoid connecting messaging bridge servers alongside untrusted MCP servers. Use MCP clients with horizontal scroll indicators or content length warnings. Implement data loss prevention (DLP) rules that flag large outbound data transfers through tool parameters. Audit tool definitions for rug pull behavior.

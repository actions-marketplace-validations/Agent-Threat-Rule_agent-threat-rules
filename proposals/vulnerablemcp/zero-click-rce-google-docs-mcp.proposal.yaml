# ATR rule proposal -- generated by scripts/sync-vulnerablemcp.ts
# Source: The Vulnerable MCP Project (vineethsai/vulnerablemcp) entry "zero-click-rce-google-docs-mcp"
# Imported on: 2026-06-12
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The advisory body is
#         attached as a YAML comment under detection.conditions; the actual
#         exploit payload usually lives at the linked references.
# Detection readiness: TRACKING_ONLY -- reference-based entry — payload lives at linked references, prose only inline
title: "Zero-Click RCE via Google Docs MCP Integration"
id: ATR-DRAFT-zero-click-rce-google-docs-mcp
rule_version: 1
status: draft
description: >
  Vulnerable MCP Project entry zero-click-rce-google-docs-mcp.
  MCP-specific prompt-injection threat, reported by Lakera AI.
  An attacker shares a malicious Google Doc containing embedded prompt injection. When Cursor fetches the document via a Google Docs MCP server, it auto-executes the injected instructions with no user interaction required. This achieves zero-click remote code execution, credential theft, and persistent access.
author: "ATR Community (vulnerablemcp sync)"
date: "2026/06/12"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: critical

references:
  vulnerablemcp_id:
    - "zero-click-rce-google-docs-mcp"
  external:
    - "https://www.lakera.ai/blog/zero-click-remote-code-execution-exploiting-mcp-agentic-ides"


metadata_provenance:
  vulnerablemcp: vulnerablemcp-sync

tags:
  category: prompt-injection
  subcategory: mcp-catalog-imported
  scan_target: mcp
  confidence: low

agent_source:
  type: tool_invocation
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the advisory mechanism below into precision-safe
    # detection.conditions regex/string matchers. The exploit payload usually
    # lives at the linked references (this is a reference-based catalog), so
    # fetch those before writing patterns. Empty until promoted to rules/.
    #
    # === advisory body (from vineethsai/vulnerablemcp) ===
    # Description:
    # An attacker shares a malicious Google Doc containing embedded prompt injection. When Cursor fetches the document via a Google Docs MCP server, it auto-executes the injected instructions with no user interaction required. This achieves zero-click remote code execution, credential theft, and persistent access.
    # 
    # How (attack mechanism):
    # The attacker creates a Google Doc with hidden prompt injection (e.g., white text on white background). They share it with the victim or place it in a shared drive. When Cursor's agent fetches document content via the Google Docs MCP server, the hidden instructions execute. These can install malicious MCP servers for persistence, exfiltrate credentials, or modify source code. The entire chain requires zero victim interaction.
    # 
    # Where:
    # The attack surface is any shared Google Doc that the MCP-connected agent can access. The vulnerability chains the Google Docs MCP server (data ingestion) with Cursor's code execution capabilities.
    # 
    # Impact:
    # Full compromise of the developer's environment with no interaction required. Attackers gain persistent access through injected MCP servers, can steal all credentials and source code, and maintain access across IDE restarts. The zero-click nature makes this exceptionally dangerous for targeted attacks against development teams.
    # 
    # Mitigation:
    # Implement prompt injection detection on all content fetched by MCP tools. Add user confirmation for any code execution triggered by external content. Isolate document-fetching MCP servers from code execution capabilities. Review shared documents in a sandboxed viewer before allowing agent access.
    []

response:
  actions:
    - block_input
    - alert
  notify:
    - security_team

confidence: 40

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: false
  reason: "reference-based entry — payload lives at linked references, prose only inline"

# === sync-vulnerablemcp.ts provenance ===
_vulnerablemcp_sync:
  entry_id: "zero-click-rce-google-docs-mcp"
  cve_ids: []
  category_source: "prompt-injection"
  category_atr: "prompt-injection"
  severity_source: "critical"
  impact_score: 10
  exploitability: "moderate"
  affected_components: ["client","server"]
  prevalence: "emerging"
  reported_by: "Lakera AI"
  disclosed_date: "2025-09-05"
  alternative_names: ["Google Docs MCP Auto-Execute","Zero-Click Agent RCE"]
  tags: ["prompt-injection","remote-code-execution","data-exfiltration","credential-theft","zero-click"]
  cisco_objectives: ["goal-hijacking","privilege-escalation","data-privacy-violation"]
  primary_url: "https://www.lakera.ai/blog/zero-click-remote-code-execution-exploiting-mcp-agentic-ides"
  imported_at: "2026-06-12T03:06:38.079Z"
  data_source: "https://raw.githubusercontent.com/vineethsai/vulnerablemcp/main/data/vulnerabilities.json"
  advisory_body: |
    Description:
    An attacker shares a malicious Google Doc containing embedded prompt injection. When Cursor fetches the document via a Google Docs MCP server, it auto-executes the injected instructions with no user interaction required. This achieves zero-click remote code execution, credential theft, and persistent access.
    
    How (attack mechanism):
    The attacker creates a Google Doc with hidden prompt injection (e.g., white text on white background). They share it with the victim or place it in a shared drive. When Cursor's agent fetches document content via the Google Docs MCP server, the hidden instructions execute. These can install malicious MCP servers for persistence, exfiltrate credentials, or modify source code. The entire chain requires zero victim interaction.
    
    Where:
    The attack surface is any shared Google Doc that the MCP-connected agent can access. The vulnerability chains the Google Docs MCP server (data ingestion) with Cursor's code execution capabilities.
    
    Impact:
    Full compromise of the developer's environment with no interaction required. Attackers gain persistent access through injected MCP servers, can steal all credentials and source code, and maintain access across IDE restarts. The zero-click nature makes this exceptionally dangerous for targeted attacks against development teams.
    
    Mitigation:
    Implement prompt injection detection on all content fetched by MCP tools. Add user confirmation for any code execution triggered by external content. Isolate document-fetching MCP servers from code execution capabilities. Review shared documents in a sandboxed viewer before allowing agent access.

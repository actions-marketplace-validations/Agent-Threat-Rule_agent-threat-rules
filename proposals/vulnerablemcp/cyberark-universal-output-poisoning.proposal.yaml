# ATR rule proposal -- generated by scripts/sync-vulnerablemcp.ts
# Source: The Vulnerable MCP Project (vineethsai/vulnerablemcp) entry "cyberark-universal-output-poisoning"
# Imported on: 2026-06-12
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The advisory body is
#         attached as a YAML comment under detection.conditions; the actual
#         exploit payload usually lives at the linked references.
# Detection readiness: TRACKING_ONLY -- reference-based entry — payload lives at linked references, prose only inline
title: "Universal Output Poisoning: No MCP Output Is Safe"
id: ATR-DRAFT-cyberark-universal-output-poisoning
rule_version: 1
status: draft
description: >
  Vulnerable MCP Project entry cyberark-universal-output-poisoning.
  MCP-specific prompt-injection threat, reported by CyberArk Threat Research.
  All MCP server output vectors—return values, error messages, metadata, resource content, and logging—can carry hidden prompt injection payloads. This demonstrates that no output channel from an MCP server is safe from injection.
author: "ATR Community (vulnerablemcp sync)"
date: "2026/06/12"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: high

references:
  vulnerablemcp_id:
    - "cyberark-universal-output-poisoning"
  external:
    - "https://www.cyberark.com/resources/threat-research-blog/poison-everywhere-no-output-from-your-mcp-server-is-safe"


metadata_provenance:
  vulnerablemcp: vulnerablemcp-sync

tags:
  category: prompt-injection
  subcategory: mcp-catalog-imported
  scan_target: mcp
  confidence: low

agent_source:
  type: tool_invocation
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the advisory mechanism below into precision-safe
    # detection.conditions regex/string matchers. The exploit payload usually
    # lives at the linked references (this is a reference-based catalog), so
    # fetch those before writing patterns. Empty until promoted to rules/.
    #
    # === advisory body (from vineethsai/vulnerablemcp) ===
    # Description:
    # All MCP server output vectors—return values, error messages, metadata, resource content, and logging—can carry hidden prompt injection payloads. This demonstrates that no output channel from an MCP server is safe from injection.
    # 
    # How (attack mechanism):
    # CyberArk demonstrated injection through five channels: (1) Tool return values containing hidden instructions, (2) Error messages with embedded prompts that execute when the LLM processes the error, (3) Resource metadata with injected content, (4) Resource content bodies with hidden text, and (5) Log and notification channels. Each channel was confirmed to successfully inject instructions into the LLM context.
    # 
    # Where:
    # The vulnerability is systemic across all MCP communication channels. Every response field that an MCP server can populate (tool results, error descriptions, resource metadata, log messages) is processed by the LLM and can influence its behavior.
    # 
    # Impact:
    # Since every output channel is exploitable, defense cannot focus on a single vector. A compromised or malicious server can inject instructions through whichever channel is most likely to evade detection. This undermines output sanitization approaches that only clean specific fields, demonstrating the need for comprehensive content security.
    # 
    # Mitigation:
    # Implement comprehensive output sanitization across ALL MCP response fields, not just tool results. Deploy prompt injection detection on every piece of data flowing from server to client. Use structured output schemas that separate data from instructions. Consider output-isolation architectures where server responses are processed in a sandboxed context.
    []

response:
  actions:
    - block_input
    - alert
  notify:
    - security_team

confidence: 40

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: false
  reason: "reference-based entry — payload lives at linked references, prose only inline"

# === sync-vulnerablemcp.ts provenance ===
_vulnerablemcp_sync:
  entry_id: "cyberark-universal-output-poisoning"
  cve_ids: []
  category_source: "prompt-injection"
  category_atr: "prompt-injection"
  severity_source: "high"
  impact_score: 8
  exploitability: "easy"
  affected_components: ["server","protocol"]
  prevalence: "widespread"
  reported_by: "CyberArk Threat Research"
  disclosed_date: "2025-07-18"
  alternative_names: ["Poison Everywhere","MCP Output Channel Injection"]
  tags: ["prompt-injection","hidden-instructions","tool-manipulation"]
  cisco_objectives: ["goal-hijacking","integrity-compromise"]
  primary_url: "https://www.cyberark.com/resources/threat-research-blog/poison-everywhere-no-output-from-your-mcp-server-is-safe"
  imported_at: "2026-06-12T03:06:38.161Z"
  data_source: "https://raw.githubusercontent.com/vineethsai/vulnerablemcp/main/data/vulnerabilities.json"
  advisory_body: |
    Description:
    All MCP server output vectors—return values, error messages, metadata, resource content, and logging—can carry hidden prompt injection payloads. This demonstrates that no output channel from an MCP server is safe from injection.
    
    How (attack mechanism):
    CyberArk demonstrated injection through five channels: (1) Tool return values containing hidden instructions, (2) Error messages with embedded prompts that execute when the LLM processes the error, (3) Resource metadata with injected content, (4) Resource content bodies with hidden text, and (5) Log and notification channels. Each channel was confirmed to successfully inject instructions into the LLM context.
    
    Where:
    The vulnerability is systemic across all MCP communication channels. Every response field that an MCP server can populate (tool results, error descriptions, resource metadata, log messages) is processed by the LLM and can influence its behavior.
    
    Impact:
    Since every output channel is exploitable, defense cannot focus on a single vector. A compromised or malicious server can inject instructions through whichever channel is most likely to evade detection. This undermines output sanitization approaches that only clean specific fields, demonstrating the need for comprehensive content security.
    
    Mitigation:
    Implement comprehensive output sanitization across ALL MCP response fields, not just tool results. Deploy prompt injection detection on every piece of data flowing from server to client. Use structured output schemas that separate data from instructions. Consider output-isolation architectures where server responses are processed in a sandboxed context.

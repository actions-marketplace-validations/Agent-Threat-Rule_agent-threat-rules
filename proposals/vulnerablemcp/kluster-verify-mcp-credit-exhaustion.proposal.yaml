# ATR rule proposal -- generated by scripts/sync-vulnerablemcp.ts
# Source: The Vulnerable MCP Project (vineethsai/vulnerablemcp) entry "kluster-verify-mcp-credit-exhaustion"
# Imported on: 2026-06-12
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The advisory body is
#         attached as a YAML comment under detection.conditions; the actual
#         exploit payload usually lives at the linked references.
# Detection readiness: TRACKING_ONLY -- reference-based entry — payload lives at linked references, prose only inline
title: "Kluster Verify MCP Server Credit Exhaustion"
id: ATR-DRAFT-kluster-verify-mcp-credit-exhaustion
rule_version: 1
status: draft
description: >
  Vulnerable MCP Project entry kluster-verify-mcp-credit-exhaustion.
  MCP-specific privilege-escalation threat, reported by mcpsec.dev.
  The Kluster Verify MCP server allows attackers to drain verification credits through unauthorized access to its MCP tools, causing financial impact and denial of service for legitimate verification operations.
author: "ATR Community (vulnerablemcp sync)"
date: "2026/06/12"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: medium

references:
  vulnerablemcp_id:
    - "kluster-verify-mcp-credit-exhaustion"
  external:
    - "https://mcpsec.dev/advisories/2025-10-16-kluster-dns-rebinding/"


metadata_provenance:
  vulnerablemcp: vulnerablemcp-sync

tags:
  category: privilege-escalation
  subcategory: mcp-catalog-imported
  scan_target: mcp
  confidence: low

agent_source:
  type: tool_invocation
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the advisory mechanism below into precision-safe
    # detection.conditions regex/string matchers. The exploit payload usually
    # lives at the linked references (this is a reference-based catalog), so
    # fetch those before writing patterns. Empty until promoted to rules/.
    #
    # === advisory body (from vineethsai/vulnerablemcp) ===
    # Description:
    # The Kluster Verify MCP server allows attackers to drain verification credits through unauthorized access to its MCP tools, causing financial impact and denial of service for legitimate verification operations.
    # 
    # How (attack mechanism):
    # An attacker accesses the Kluster Verify MCP server's tools (either through network exposure or prompt injection) and repeatedly invokes verification operations. Each verification consumes credits from the organization's account. Automated exploitation can rapidly exhaust the entire credit balance.
    # 
    # Where:
    # The vulnerability is in the Kluster Verify MCP server's access control. The verification tools are accessible without proper authorization, allowing unauthorized credit consumption.
    # 
    # Impact:
    # Financial loss through credit exhaustion. Denial of service for legitimate verification operations until credits are replenished. Potential for verified identity data leakage through the verification tool responses.
    # 
    # Mitigation:
    # Add authentication to the MCP server endpoint. Implement rate limiting on verification operations. Set up credit usage alerts and automatic cutoffs. Bind the server to localhost if only local access is needed.
    []

response:
  actions:
    - alert
  notify:
    - security_team

confidence: 40

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: false
  reason: "reference-based entry — payload lives at linked references, prose only inline"

# === sync-vulnerablemcp.ts provenance ===
_vulnerablemcp_sync:
  entry_id: "kluster-verify-mcp-credit-exhaustion"
  cve_ids: []
  category_source: "authentication"
  category_atr: "privilege-escalation"
  severity_source: "medium"
  impact_score: 5
  exploitability: "easy"
  affected_components: ["server"]
  prevalence: "rare"
  reported_by: "mcpsec.dev"
  disclosed_date: "2025-10-16"
  alternative_names: ["Kluster MCP Resource Drain"]
  tags: ["resource-theft","security-bypass"]
  cisco_objectives: ["unauthorized-access"]
  primary_url: "https://mcpsec.dev/advisories/2025-10-16-kluster-dns-rebinding/"
  imported_at: "2026-06-12T03:06:38.289Z"
  data_source: "https://raw.githubusercontent.com/vineethsai/vulnerablemcp/main/data/vulnerabilities.json"
  advisory_body: |
    Description:
    The Kluster Verify MCP server allows attackers to drain verification credits through unauthorized access to its MCP tools, causing financial impact and denial of service for legitimate verification operations.
    
    How (attack mechanism):
    An attacker accesses the Kluster Verify MCP server's tools (either through network exposure or prompt injection) and repeatedly invokes verification operations. Each verification consumes credits from the organization's account. Automated exploitation can rapidly exhaust the entire credit balance.
    
    Where:
    The vulnerability is in the Kluster Verify MCP server's access control. The verification tools are accessible without proper authorization, allowing unauthorized credit consumption.
    
    Impact:
    Financial loss through credit exhaustion. Denial of service for legitimate verification operations until credits are replenished. Potential for verified identity data leakage through the verification tool responses.
    
    Mitigation:
    Add authentication to the MCP server endpoint. Implement rate limiting on verification operations. Set up credit usage alerts and automatic cutoffs. Bind the server to localhost if only local access is needed.

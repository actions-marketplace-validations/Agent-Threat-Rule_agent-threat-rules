# ATR rule proposal -- generated by scripts/sync-vulnerablemcp.ts
# Source: The Vulnerable MCP Project (vineethsai/vulnerablemcp) entry "cursor-browser-injection-rogue-mcp"
# Imported on: 2026-06-12
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The advisory body is
#         attached as a YAML comment under detection.conditions; the actual
#         exploit payload usually lives at the linked references.
# Detection readiness: TRACKING_ONLY -- reference-based entry — payload lives at linked references, prose only inline
title: "Rogue MCP Server Browser Injection in Cursor"
id: ATR-DRAFT-cursor-browser-injection-rogue-mcp
rule_version: 1
status: draft
description: >
  Vulnerable MCP Project entry cursor-browser-injection-rogue-mcp.
  MCP-specific tool-poisoning threat, reported by 0xrelogic (Security Researcher).
  A malicious MCP server can inject JavaScript into Cursor's built-in browser. Cursor lacks integrity checks on runtime components loaded through MCP interactions, enabling arbitrary code execution within the IDE's browser context.
author: "ATR Community (vulnerablemcp sync)"
date: "2026/06/12"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: high

references:
  vulnerablemcp_id:
    - "cursor-browser-injection-rogue-mcp"
  external:
    - "https://dev.to/0xrelogic/mcp-x-cursor-poc-rogue-mcp-servers-ide-browsers-and-real-defenses-1dda"


metadata_provenance:
  vulnerablemcp: vulnerablemcp-sync

tags:
  category: tool-poisoning
  subcategory: mcp-catalog-imported
  scan_target: mcp
  confidence: low

agent_source:
  type: tool_invocation
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the advisory mechanism below into precision-safe
    # detection.conditions regex/string matchers. The exploit payload usually
    # lives at the linked references (this is a reference-based catalog), so
    # fetch those before writing patterns. Empty until promoted to rules/.
    #
    # === advisory body (from vineethsai/vulnerablemcp) ===
    # Description:
    # A malicious MCP server can inject JavaScript into Cursor's built-in browser. Cursor lacks integrity checks on runtime components loaded through MCP interactions, enabling arbitrary code execution within the IDE's browser context.
    # 
    # How (attack mechanism):
    # A rogue MCP server returns specially crafted responses containing JavaScript payloads. When Cursor processes these responses and renders them in its built-in browser component, the JavaScript executes within the IDE's browser context. The lack of integrity checks on runtime components means the injected code runs with the browser's full privileges within the IDE.
    # 
    # Where:
    # The vulnerability is in Cursor's built-in browser component and its interaction with MCP server responses. The IDE does not validate or sanitize content that flows into its browser runtime.
    # 
    # Impact:
    # Arbitrary JavaScript execution within Cursor's browser context. Depending on the browser's privilege level, this can lead to access to IDE APIs, file system operations, credential theft from browser storage, and potential escalation to full IDE compromise. The attack demonstrates that MCP server responses can escape their intended sandbox.
    # 
    # Mitigation:
    # Implement Content Security Policy (CSP) for Cursor's built-in browser. Sanitize all MCP server output before rendering in browser contexts. Add integrity checks for runtime components. Isolate the browser component from IDE-privileged APIs. Restrict what content MCP servers can inject into rendered views.
    []

response:
  actions:
    - block_input
    - alert
  notify:
    - security_team

confidence: 40

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: false
  reason: "reference-based entry — payload lives at linked references, prose only inline"

# === sync-vulnerablemcp.ts provenance ===
_vulnerablemcp_sync:
  entry_id: "cursor-browser-injection-rogue-mcp"
  cve_ids: []
  category_source: "input-validation"
  category_atr: "tool-poisoning"
  severity_source: "high"
  impact_score: 8
  exploitability: "moderate"
  affected_components: ["client"]
  prevalence: "emerging"
  reported_by: "0xrelogic (Security Researcher)"
  disclosed_date: "2025-07-22"
  alternative_names: ["Cursor IDE Browser Code Injection","MCP-to-Browser Attack"]
  tags: ["code-execution","security-bypass","browser-injection"]
  cisco_objectives: ["privilege-escalation","unauthorized-access"]
  primary_url: "https://dev.to/0xrelogic/mcp-x-cursor-poc-rogue-mcp-servers-ide-browsers-and-real-defenses-1dda"
  imported_at: "2026-06-12T03:06:38.229Z"
  data_source: "https://raw.githubusercontent.com/vineethsai/vulnerablemcp/main/data/vulnerabilities.json"
  advisory_body: |
    Description:
    A malicious MCP server can inject JavaScript into Cursor's built-in browser. Cursor lacks integrity checks on runtime components loaded through MCP interactions, enabling arbitrary code execution within the IDE's browser context.
    
    How (attack mechanism):
    A rogue MCP server returns specially crafted responses containing JavaScript payloads. When Cursor processes these responses and renders them in its built-in browser component, the JavaScript executes within the IDE's browser context. The lack of integrity checks on runtime components means the injected code runs with the browser's full privileges within the IDE.
    
    Where:
    The vulnerability is in Cursor's built-in browser component and its interaction with MCP server responses. The IDE does not validate or sanitize content that flows into its browser runtime.
    
    Impact:
    Arbitrary JavaScript execution within Cursor's browser context. Depending on the browser's privilege level, this can lead to access to IDE APIs, file system operations, credential theft from browser storage, and potential escalation to full IDE compromise. The attack demonstrates that MCP server responses can escape their intended sandbox.
    
    Mitigation:
    Implement Content Security Policy (CSP) for Cursor's built-in browser. Sanitize all MCP server output before rendering in browser contexts. Add integrity checks for runtime components. Isolate the browser component from IDE-privileged APIs. Restrict what content MCP servers can inject into rendered views.

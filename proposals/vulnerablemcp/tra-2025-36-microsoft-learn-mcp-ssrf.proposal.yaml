# ATR rule proposal -- generated by scripts/sync-vulnerablemcp.ts
# Source: The Vulnerable MCP Project (vineethsai/vulnerablemcp) entry "tra-2025-36-microsoft-learn-mcp-ssrf"
# Imported on: 2026-06-12
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The advisory body is
#         attached as a YAML comment under detection.conditions; the actual
#         exploit payload usually lives at the linked references.
# Detection readiness: TRACKING_ONLY -- reference-based entry — payload lives at linked references, prose only inline
title: "Microsoft Learn MCP Server SSRF (TRA-2025-36)"
id: ATR-DRAFT-tra-2025-36-microsoft-learn-mcp-ssrf
rule_version: 1
status: draft
description: >
  Vulnerable MCP Project entry tra-2025-36-microsoft-learn-mcp-ssrf.
  MCP-specific tool-poisoning threat, reported by Tenable Research.
  The microsoft_docs_fetch tool in the Microsoft Learn MCP Server lacks URL validation, allowing requests to any host instead of restricting to microsoft.com domains. This enables SSRF attacks through the MCP server.
author: "ATR Community (vulnerablemcp sync)"
date: "2026/06/12"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: high

references:
  vulnerablemcp_id:
    - "tra-2025-36-microsoft-learn-mcp-ssrf"
  external:
    - "https://www.tenable.com/security/research/tra-2025-36"


metadata_provenance:
  vulnerablemcp: vulnerablemcp-sync

tags:
  category: tool-poisoning
  subcategory: mcp-catalog-imported
  scan_target: mcp
  confidence: low

agent_source:
  type: tool_invocation
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the advisory mechanism below into precision-safe
    # detection.conditions regex/string matchers. The exploit payload usually
    # lives at the linked references (this is a reference-based catalog), so
    # fetch those before writing patterns. Empty until promoted to rules/.
    #
    # === advisory body (from vineethsai/vulnerablemcp) ===
    # Description:
    # The microsoft_docs_fetch tool in the Microsoft Learn MCP Server lacks URL validation, allowing requests to any host instead of restricting to microsoft.com domains. This enables SSRF attacks through the MCP server.
    # 
    # How (attack mechanism):
    # The microsoft_docs_fetch tool accepts a URL parameter intended to fetch Microsoft documentation pages. However, no validation ensures the URL points to a microsoft.com domain. An attacker (or a prompt injection payload) can direct the tool to fetch arbitrary URLs, including internal network resources, cloud metadata endpoints, or attacker-controlled servers.
    # 
    # Where:
    # The vulnerability is in the microsoft_docs_fetch tool implementation, which is intended to fetch documentation from Microsoft Learn but accepts arbitrary URLs without domain validation.
    # 
    # Impact:
    # SSRF allows access to internal network services, cloud infrastructure metadata, and can be used to exfiltrate data through the MCP server. Since the request originates from the server, it may bypass firewall rules and access controls designed to block external requests.
    # 
    # Mitigation:
    # Implement strict URL allowlisting in the microsoft_docs_fetch tool to only permit requests to *.microsoft.com and *.azure.com domains. Add network-level egress controls. Validate URL schemes (only allow https). Block requests to private IP ranges and cloud metadata endpoints.
    []

response:
  actions:
    - block_input
    - alert
  notify:
    - security_team

confidence: 40

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: false
  reason: "reference-based entry — payload lives at linked references, prose only inline"

# === sync-vulnerablemcp.ts provenance ===
_vulnerablemcp_sync:
  entry_id: "tra-2025-36-microsoft-learn-mcp-ssrf"
  cve_ids: []
  category_source: "input-validation"
  category_atr: "tool-poisoning"
  severity_source: "high"
  impact_score: 7
  exploitability: "easy"
  affected_components: ["server"]
  prevalence: "emerging"
  reported_by: "Tenable Research"
  disclosed_date: "2025-09-19"
  alternative_names: ["microsoft_docs_fetch SSRF","MS Learn MCP URL Bypass"]
  tags: ["ssrf","security-bypass"]
  cisco_objectives: ["unauthorized-access","data-privacy-violation"]
  primary_url: "https://www.tenable.com/security/research/tra-2025-36"
  imported_at: "2026-06-12T03:06:38.079Z"
  data_source: "https://raw.githubusercontent.com/vineethsai/vulnerablemcp/main/data/vulnerabilities.json"
  advisory_body: |
    Description:
    The microsoft_docs_fetch tool in the Microsoft Learn MCP Server lacks URL validation, allowing requests to any host instead of restricting to microsoft.com domains. This enables SSRF attacks through the MCP server.
    
    How (attack mechanism):
    The microsoft_docs_fetch tool accepts a URL parameter intended to fetch Microsoft documentation pages. However, no validation ensures the URL points to a microsoft.com domain. An attacker (or a prompt injection payload) can direct the tool to fetch arbitrary URLs, including internal network resources, cloud metadata endpoints, or attacker-controlled servers.
    
    Where:
    The vulnerability is in the microsoft_docs_fetch tool implementation, which is intended to fetch documentation from Microsoft Learn but accepts arbitrary URLs without domain validation.
    
    Impact:
    SSRF allows access to internal network services, cloud infrastructure metadata, and can be used to exfiltrate data through the MCP server. Since the request originates from the server, it may bypass firewall rules and access controls designed to block external requests.
    
    Mitigation:
    Implement strict URL allowlisting in the microsoft_docs_fetch tool to only permit requests to *.microsoft.com and *.azure.com domains. Add network-level egress controls. Validate URL schemes (only allow https). Block requests to private IP ranges and cloud metadata endpoints.

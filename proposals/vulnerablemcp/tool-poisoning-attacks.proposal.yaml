# ATR rule proposal -- generated by scripts/sync-vulnerablemcp.ts
# Source: The Vulnerable MCP Project (vineethsai/vulnerablemcp) entry "tool-poisoning-attacks"
# Imported on: 2026-06-12
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The advisory body is
#         attached as a YAML comment under detection.conditions; the actual
#         exploit payload usually lives at the linked references.
# Detection readiness: READY -- payload-like content inline in description/how/where/impact
title: "Tool Poisoning Attacks"
id: ATR-DRAFT-tool-poisoning-attacks
rule_version: 1
status: draft
description: >
  Vulnerable MCP Project entry tool-poisoning-attacks.
  MCP-specific prompt-injection threat, reported by Invariant Labs.
  A specialized form of prompt injection where malicious instructions are embedded in tool descriptions—visible to the LLM but hidden from users. Attackers create tools with concealed directives that cause the LLM to perform unauthorized actions such as exfiltrating private data.
author: "ATR Community (vulnerablemcp sync)"
date: "2026/06/12"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: critical

references:
  vulnerablemcp_id:
    - "tool-poisoning-attacks"
  external:
    - "https://invariantlabs.ai/blog/mcp-security-notification-tool-poisoning-attacks"
    - "https://blog.trailofbits.com/2025/04/21/jumping-the-line-how-mcp-servers-can-attack-you-before-you-ever-use-them/"


metadata_provenance:
  vulnerablemcp: vulnerablemcp-sync

tags:
  category: prompt-injection
  subcategory: mcp-catalog-imported
  scan_target: mcp
  confidence: high

agent_source:
  type: tool_invocation
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the advisory mechanism below into precision-safe
    # detection.conditions regex/string matchers. The exploit payload usually
    # lives at the linked references (this is a reference-based catalog), so
    # fetch those before writing patterns. Empty until promoted to rules/.
    #
    # === advisory body (from vineethsai/vulnerablemcp) ===
    # Description:
    # A specialized form of prompt injection where malicious instructions are embedded in tool descriptions—visible to the LLM but hidden from users. Attackers create tools with concealed directives that cause the LLM to perform unauthorized actions such as exfiltrating private data.
    # 
    # How (attack mechanism):
    # A malicious MCP server defines a tool (e.g., a calculator) with hidden instructions in its description telling the LLM to read sensitive files (like ~/.ssh/id_rsa) and pass their contents as a parameter. The exfiltrated data is sent to the attacker's server before the tool returns its normal result. Users see only the calculator output and never the hidden data transfer.
    # 
    # Where:
    # The vulnerability exists in the tool description field of MCP server tool definitions. Since MCP clients inject all tool descriptions into the LLM context window, any connected server can influence model behavior through this channel.
    # 
    # Impact:
    # Complete compromise of user data accessible to the LLM. Attackers can silently exfiltrate files, environment variables, conversation history, and any data the MCP client has permission to access. The attack is invisible to users who don't inspect raw tool descriptions.
    # 
    # Mitigation:
    # Inspect all tool descriptions before connecting to new MCP servers. Use MCP clients that display full tool descriptions. Implement tool description scanning for suspicious instructions. Sandbox MCP server access to limit file system and network exposure. Consider using ETDI (Enhanced Tool Definition Interface) for tool integrity verification.
    []

response:
  actions:
    - block_input
    - alert
  notify:
    - security_team

confidence: 70

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: true
  reason: "payload-like content inline in description/how/where/impact"

# === sync-vulnerablemcp.ts provenance ===
_vulnerablemcp_sync:
  entry_id: "tool-poisoning-attacks"
  cve_ids: []
  category_source: "prompt-injection"
  category_atr: "prompt-injection"
  severity_source: "critical"
  impact_score: 9
  exploitability: "easy"
  affected_components: ["server","client"]
  prevalence: "widespread"
  reported_by: "Invariant Labs"
  disclosed_date: "2025-04-01"
  alternative_names: ["Tool Description Injection","MCP Tool Prompt Injection"]
  tags: ["data-exfiltration","prompt-injection","hidden-instructions"]
  cisco_objectives: ["goal-hijacking","data-privacy-violation"]
  primary_url: "https://invariantlabs.ai/blog/mcp-security-notification-tool-poisoning-attacks"
  imported_at: "2026-06-12T03:06:37.890Z"
  data_source: "https://raw.githubusercontent.com/vineethsai/vulnerablemcp/main/data/vulnerabilities.json"
  advisory_body: |
    Description:
    A specialized form of prompt injection where malicious instructions are embedded in tool descriptions—visible to the LLM but hidden from users. Attackers create tools with concealed directives that cause the LLM to perform unauthorized actions such as exfiltrating private data.
    
    How (attack mechanism):
    A malicious MCP server defines a tool (e.g., a calculator) with hidden instructions in its description telling the LLM to read sensitive files (like ~/.ssh/id_rsa) and pass their contents as a parameter. The exfiltrated data is sent to the attacker's server before the tool returns its normal result. Users see only the calculator output and never the hidden data transfer.
    
    Where:
    The vulnerability exists in the tool description field of MCP server tool definitions. Since MCP clients inject all tool descriptions into the LLM context window, any connected server can influence model behavior through this channel.
    
    Impact:
    Complete compromise of user data accessible to the LLM. Attackers can silently exfiltrate files, environment variables, conversation history, and any data the MCP client has permission to access. The attack is invisible to users who don't inspect raw tool descriptions.
    
    Mitigation:
    Inspect all tool descriptions before connecting to new MCP servers. Use MCP clients that display full tool descriptions. Implement tool description scanning for suspicious instructions. Sandbox MCP server access to limit file system and network exposure. Consider using ETDI (Enhanced Tool Definition Interface) for tool integrity verification.

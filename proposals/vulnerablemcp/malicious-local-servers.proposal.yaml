# ATR rule proposal -- generated by scripts/sync-vulnerablemcp.ts
# Source: The Vulnerable MCP Project (vineethsai/vulnerablemcp) entry "malicious-local-servers"
# Imported on: 2026-06-12
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The advisory body is
#         attached as a YAML comment under detection.conditions; the actual
#         exploit payload usually lives at the linked references.
# Detection readiness: TRACKING_ONLY -- reference-based entry — payload lives at linked references, prose only inline
title: "Malicious Local MCP Servers"
id: ATR-DRAFT-malicious-local-servers
rule_version: 1
status: draft
description: >
  Vulnerable MCP Project entry malicious-local-servers.
  MCP-specific tool-poisoning threat, reported by Shrivu Shankar.
  MCP's stdio transport enables frictionless local server execution, creating a low-barrier path for users to download and run potentially malicious third-party code on their machines with full local privileges.
author: "ATR Community (vulnerablemcp sync)"
date: "2026/06/12"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: medium

references:
  vulnerablemcp_id:
    - "malicious-local-servers"
  external:
    - "https://blog.sshh.io/p/everything-wrong-with-mcp"


metadata_provenance:
  vulnerablemcp: vulnerablemcp-sync

tags:
  category: tool-poisoning
  subcategory: mcp-catalog-imported
  scan_target: mcp
  confidence: low

agent_source:
  type: tool_invocation
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the advisory mechanism below into precision-safe
    # detection.conditions regex/string matchers. The exploit payload usually
    # lives at the linked references (this is a reference-based catalog), so
    # fetch those before writing patterns. Empty until promoted to rules/.
    #
    # === advisory body (from vineethsai/vulnerablemcp) ===
    # Description:
    # MCP's stdio transport enables frictionless local server execution, creating a low-barrier path for users to download and run potentially malicious third-party code on their machines with full local privileges.
    # 
    # How (attack mechanism):
    # Attackers publish an appealing MCP server (e.g., an AI-powered productivity tool) on GitHub or npm. Installation instructions tell users to clone and run the code locally. The server runs with the user's full permissions and can access files, network, and system resources. Malicious code can execute alongside legitimate MCP functionality.
    # 
    # Where:
    # The risk exists in the MCP ecosystem's distribution model. MCP servers are typically distributed as source code or npm packages that run directly on the user's machine via stdio transport with full local filesystem and network access.
    # 
    # Impact:
    # Full local system compromise including file access, credential theft, cryptocurrency wallet exfiltration, keylogging, and persistent backdoor installation. The stdio transport provides no sandboxing or permission boundaries between the server and the host system.
    # 
    # Mitigation:
    # Run MCP servers in containers or sandboxed environments. Review server source code before installation. Prefer servers from verified publishers. Use operating system-level permission restrictions. Monitor MCP server network activity for unexpected connections.
    []

response:
  actions:
    - alert
  notify:
    - security_team

confidence: 40

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: false
  reason: "reference-based entry — payload lives at linked references, prose only inline"

# === sync-vulnerablemcp.ts provenance ===
_vulnerablemcp_sync:
  entry_id: "malicious-local-servers"
  cve_ids: []
  category_source: "trust-model"
  category_atr: "tool-poisoning"
  severity_source: "medium"
  impact_score: 6
  exploitability: "easy"
  affected_components: ["server","ecosystem"]
  prevalence: "common"
  reported_by: "Shrivu Shankar"
  disclosed_date: "2025-04-13"
  alternative_names: ["Stdio Transport Code Execution","Local Server Trust Abuse"]
  tags: ["code-execution","supply-chain","social-engineering"]
  cisco_objectives: ["supply-chain-compromise","privilege-escalation"]
  primary_url: "https://blog.sshh.io/p/everything-wrong-with-mcp"
  imported_at: "2026-06-12T03:06:38.008Z"
  data_source: "https://raw.githubusercontent.com/vineethsai/vulnerablemcp/main/data/vulnerabilities.json"
  advisory_body: |
    Description:
    MCP's stdio transport enables frictionless local server execution, creating a low-barrier path for users to download and run potentially malicious third-party code on their machines with full local privileges.
    
    How (attack mechanism):
    Attackers publish an appealing MCP server (e.g., an AI-powered productivity tool) on GitHub or npm. Installation instructions tell users to clone and run the code locally. The server runs with the user's full permissions and can access files, network, and system resources. Malicious code can execute alongside legitimate MCP functionality.
    
    Where:
    The risk exists in the MCP ecosystem's distribution model. MCP servers are typically distributed as source code or npm packages that run directly on the user's machine via stdio transport with full local filesystem and network access.
    
    Impact:
    Full local system compromise including file access, credential theft, cryptocurrency wallet exfiltration, keylogging, and persistent backdoor installation. The stdio transport provides no sandboxing or permission boundaries between the server and the host system.
    
    Mitigation:
    Run MCP servers in containers or sandboxed environments. Review server source code before installation. Prefer servers from verified publishers. Use operating system-level permission restrictions. Monitor MCP server network activity for unexpected connections.

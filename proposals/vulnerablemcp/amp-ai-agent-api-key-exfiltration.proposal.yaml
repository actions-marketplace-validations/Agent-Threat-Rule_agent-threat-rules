# ATR rule proposal -- generated by scripts/sync-vulnerablemcp.ts
# Source: The Vulnerable MCP Project (vineethsai/vulnerablemcp) entry "amp-ai-agent-api-key-exfiltration"
# Imported on: 2026-06-12
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The advisory body is
#         attached as a YAML comment under detection.conditions; the actual
#         exploit payload usually lives at the linked references.
# Detection readiness: TRACKING_ONLY -- reference-based entry — payload lives at linked references, prose only inline
title: "Amp AI Agent API Key Exfiltration via Prompt Injection"
id: ATR-DRAFT-amp-ai-agent-api-key-exfiltration
rule_version: 1
status: draft
description: >
  Vulnerable MCP Project entry amp-ai-agent-api-key-exfiltration.
  MCP-specific prompt-injection threat, reported by mcpsec.dev.
  Prompt injection attacks against the Amp AI Agent can exfiltrate API keys from the agent's environment. Malicious content processed by the agent extracts credentials and sends them to attacker-controlled endpoints.
author: "ATR Community (vulnerablemcp sync)"
date: "2026/06/12"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: high

references:
  vulnerablemcp_id:
    - "amp-ai-agent-api-key-exfiltration"
  external:
    - "https://mcpsec.dev/advisories/2025-10-03-amp-dns-exfiltration-prompt-injection/"


metadata_provenance:
  vulnerablemcp: vulnerablemcp-sync

tags:
  category: prompt-injection
  subcategory: mcp-catalog-imported
  scan_target: mcp
  confidence: low

agent_source:
  type: tool_invocation
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the advisory mechanism below into precision-safe
    # detection.conditions regex/string matchers. The exploit payload usually
    # lives at the linked references (this is a reference-based catalog), so
    # fetch those before writing patterns. Empty until promoted to rules/.
    #
    # === advisory body (from vineethsai/vulnerablemcp) ===
    # Description:
    # Prompt injection attacks against the Amp AI Agent can exfiltrate API keys from the agent's environment. Malicious content processed by the agent extracts credentials and sends them to attacker-controlled endpoints.
    # 
    # How (attack mechanism):
    # An attacker embeds prompt injection in content that the Amp AI Agent will process (code comments, documentation, issue descriptions). The injected instructions direct the agent to read API keys from its environment or configuration and exfiltrate them through MCP tool calls, HTTP requests, or by encoding them in seemingly innocuous outputs.
    # 
    # Where:
    # The vulnerability is in the Amp AI Agent's handling of its credential environment. API keys and tokens accessible in the agent's context can be extracted via prompt injection in processed content.
    # 
    # Impact:
    # Theft of API keys for cloud services, AI providers, code repositories, and other integrations. Compromised keys enable access to the victim's accounts and infrastructure. The impact multiplies as development agents typically have access to multiple service credentials.
    # 
    # Mitigation:
    # Isolate credential storage from the agent's accessible context. Use short-lived, scoped tokens that expire quickly. Implement prompt injection detection on all input content. Monitor for unusual API key usage patterns. Never store long-lived credentials in the agent's environment variables.
    []

response:
  actions:
    - block_input
    - alert
  notify:
    - security_team

confidence: 40

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: false
  reason: "reference-based entry — payload lives at linked references, prose only inline"

# === sync-vulnerablemcp.ts provenance ===
_vulnerablemcp_sync:
  entry_id: "amp-ai-agent-api-key-exfiltration"
  cve_ids: []
  category_source: "prompt-injection"
  category_atr: "prompt-injection"
  severity_source: "high"
  impact_score: 8
  exploitability: "moderate"
  affected_components: ["client"]
  prevalence: "emerging"
  reported_by: "mcpsec.dev"
  disclosed_date: "2025-10-03"
  alternative_names: ["Amp Agent Credential Theft"]
  tags: ["prompt-injection","credential-theft","data-exfiltration"]
  cisco_objectives: ["data-privacy-violation","goal-hijacking"]
  primary_url: "https://mcpsec.dev/advisories/2025-10-03-amp-dns-exfiltration-prompt-injection/"
  imported_at: "2026-06-12T03:06:38.299Z"
  data_source: "https://raw.githubusercontent.com/vineethsai/vulnerablemcp/main/data/vulnerabilities.json"
  advisory_body: |
    Description:
    Prompt injection attacks against the Amp AI Agent can exfiltrate API keys from the agent's environment. Malicious content processed by the agent extracts credentials and sends them to attacker-controlled endpoints.
    
    How (attack mechanism):
    An attacker embeds prompt injection in content that the Amp AI Agent will process (code comments, documentation, issue descriptions). The injected instructions direct the agent to read API keys from its environment or configuration and exfiltrate them through MCP tool calls, HTTP requests, or by encoding them in seemingly innocuous outputs.
    
    Where:
    The vulnerability is in the Amp AI Agent's handling of its credential environment. API keys and tokens accessible in the agent's context can be extracted via prompt injection in processed content.
    
    Impact:
    Theft of API keys for cloud services, AI providers, code repositories, and other integrations. Compromised keys enable access to the victim's accounts and infrastructure. The impact multiplies as development agents typically have access to multiple service credentials.
    
    Mitigation:
    Isolate credential storage from the agent's accessible context. Use short-lived, scoped tokens that expire quickly. Implement prompt injection detection on all input content. Monitor for unusual API key usage patterns. Never store long-lived credentials in the agent's environment variables.

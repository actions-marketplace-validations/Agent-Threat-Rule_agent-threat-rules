# ATR rule proposal -- generated by scripts/sync-vulnerablemcp.ts
# Source: The Vulnerable MCP Project (vineethsai/vulnerablemcp) entry "consent-fatigue-attacks"
# Imported on: 2026-06-12
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The advisory body is
#         attached as a YAML comment under detection.conditions; the actual
#         exploit payload usually lives at the linked references.
# Detection readiness: TRACKING_ONLY -- reference-based entry — payload lives at linked references, prose only inline
title: "Consent Fatigue Attacks"
id: ATR-DRAFT-consent-fatigue-attacks
rule_version: 1
status: draft
description: >
  Vulnerable MCP Project entry consent-fatigue-attacks.
  MCP-specific privilege-escalation threat, reported by Palo Alto Networks.
  Malicious MCP servers trigger repeated consent requests to fatigue users into granting excessive permissions, exploiting human psychology to bypass approval-based security controls.
author: "ATR Community (vulnerablemcp sync)"
date: "2026/06/12"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: medium

references:
  vulnerablemcp_id:
    - "consent-fatigue-attacks"
  external:
    - "https://live.paloaltonetworks.com/t5/community-blogs/mcp-security-exposed-what-you-need-to-know-now/ba-p/1227143"


metadata_provenance:
  vulnerablemcp: vulnerablemcp-sync

tags:
  category: privilege-escalation
  subcategory: mcp-catalog-imported
  scan_target: mcp
  confidence: low

agent_source:
  type: tool_invocation
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the advisory mechanism below into precision-safe
    # detection.conditions regex/string matchers. The exploit payload usually
    # lives at the linked references (this is a reference-based catalog), so
    # fetch those before writing patterns. Empty until promoted to rules/.
    #
    # === advisory body (from vineethsai/vulnerablemcp) ===
    # Description:
    # Malicious MCP servers trigger repeated consent requests to fatigue users into granting excessive permissions, exploiting human psychology to bypass approval-based security controls.
    # 
    # How (attack mechanism):
    # A malicious server deliberately triggers many approval prompts for benign-looking operations. After the user has approved dozens of routine requests, a malicious operation is slipped in. Fatigued users tend to click 'approve' reflexively, especially when the prompt looks similar to previous legitimate ones. Some variations gradually escalate permission scope.
    # 
    # Where:
    # The vulnerability exists in MCP client consent mechanisms that present individual approval dialogs for each tool operation. The attack exploits the human interface, not a technical flaw.
    # 
    # Impact:
    # Users unknowingly grant dangerous permissions including file system access, network requests to attacker-controlled servers, and execution of arbitrary commands. The social engineering aspect makes this difficult to defend against with purely technical controls.
    # 
    # Mitigation:
    # Implement MCP clients with batched approval (approve tool categories rather than individual invocations). Use risk-based approval that only prompts for high-risk operations. Set session-level permission policies. Add visual differentiation for high-risk vs routine approvals. Implement rate limiting on consent prompts.
    []

response:
  actions:
    - alert
  notify:
    - security_team

confidence: 40

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: false
  reason: "reference-based entry — payload lives at linked references, prose only inline"

# === sync-vulnerablemcp.ts provenance ===
_vulnerablemcp_sync:
  entry_id: "consent-fatigue-attacks"
  cve_ids: []
  category_source: "authentication"
  category_atr: "privilege-escalation"
  severity_source: "medium"
  impact_score: 6
  exploitability: "easy"
  affected_components: ["client","server"]
  prevalence: "common"
  reported_by: "Palo Alto Networks"
  disclosed_date: "2025-04-01"
  alternative_names: ["Permission Fatigue Exploitation","Approval Spam Attack"]
  tags: ["social-engineering","consent-bypass","security-bypass"]
  cisco_objectives: ["unauthorized-access","goal-hijacking"]
  primary_url: "https://live.paloaltonetworks.com/t5/community-blogs/mcp-security-exposed-what-you-need-to-know-now/ba-p/1227143"
  imported_at: "2026-06-12T03:06:38.022Z"
  data_source: "https://raw.githubusercontent.com/vineethsai/vulnerablemcp/main/data/vulnerabilities.json"
  advisory_body: |
    Description:
    Malicious MCP servers trigger repeated consent requests to fatigue users into granting excessive permissions, exploiting human psychology to bypass approval-based security controls.
    
    How (attack mechanism):
    A malicious server deliberately triggers many approval prompts for benign-looking operations. After the user has approved dozens of routine requests, a malicious operation is slipped in. Fatigued users tend to click 'approve' reflexively, especially when the prompt looks similar to previous legitimate ones. Some variations gradually escalate permission scope.
    
    Where:
    The vulnerability exists in MCP client consent mechanisms that present individual approval dialogs for each tool operation. The attack exploits the human interface, not a technical flaw.
    
    Impact:
    Users unknowingly grant dangerous permissions including file system access, network requests to attacker-controlled servers, and execution of arbitrary commands. The social engineering aspect makes this difficult to defend against with purely technical controls.
    
    Mitigation:
    Implement MCP clients with batched approval (approve tool categories rather than individual invocations). Use risk-based approval that only prompts for high-risk operations. Set session-level permission policies. Add visual differentiation for high-risk vs routine approvals. Implement rate limiting on consent prompts.

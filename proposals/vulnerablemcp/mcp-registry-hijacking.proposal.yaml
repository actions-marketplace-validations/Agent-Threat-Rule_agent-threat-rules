# ATR rule proposal -- generated by scripts/sync-vulnerablemcp.ts
# Source: The Vulnerable MCP Project (vineethsai/vulnerablemcp) entry "mcp-registry-hijacking"
# Imported on: 2026-06-12
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The advisory body is
#         attached as a YAML comment under detection.conditions; the actual
#         exploit payload usually lives at the linked references.
# Detection readiness: TRACKING_ONLY -- reference-based entry — payload lives at linked references, prose only inline
title: "MCP Registry Hijacking (Supply Chain)"
id: ATR-DRAFT-mcp-registry-hijacking
rule_version: 1
status: draft
description: >
  Vulnerable MCP Project entry mcp-registry-hijacking.
  MCP-specific tool-poisoning threat, reported by Academic Research (arXiv).
  Academic analysis of 67,057 MCP servers across 6 public registries found that a substantial number can be hijacked due to lack of vetted submission processes. Untrusted servers can exfiltrate data from co-connected trusted servers through the shared agent context.
author: "ATR Community (vulnerablemcp sync)"
date: "2026/06/12"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: high

references:
  vulnerablemcp_id:
    - "mcp-registry-hijacking"
  external:
    - "https://arxiv.org/html/2510.16558v1"


metadata_provenance:
  vulnerablemcp: vulnerablemcp-sync

tags:
  category: tool-poisoning
  subcategory: mcp-catalog-imported
  scan_target: mcp
  confidence: low

agent_source:
  type: tool_invocation
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the advisory mechanism below into precision-safe
    # detection.conditions regex/string matchers. The exploit payload usually
    # lives at the linked references (this is a reference-based catalog), so
    # fetch those before writing patterns. Empty until promoted to rules/.
    #
    # === advisory body (from vineethsai/vulnerablemcp) ===
    # Description:
    # Academic analysis of 67,057 MCP servers across 6 public registries found that a substantial number can be hijacked due to lack of vetted submission processes. Untrusted servers can exfiltrate data from co-connected trusted servers through the shared agent context.
    # 
    # How (attack mechanism):
    # Attackers submit malicious MCP servers to public registries using minimal or fabricated identity information. The servers pass basic checks (valid manifest, working endpoints) but contain hidden malicious behavior. When users install these servers alongside legitimate ones, the malicious server can exploit shared agent context to intercept or manipulate calls to trusted servers, exfiltrate data, or inject instructions.
    # 
    # Where:
    # The vulnerability is in MCP server registries (directories where users discover and install servers). Six major public registries were analyzed and all were found to lack adequate submission vetting, identity verification, and integrity controls.
    # 
    # Impact:
    # Large-scale supply chain compromise of the MCP ecosystem. Users who discover servers through registries have no reliable way to distinguish malicious from legitimate servers. The shared agent context means a single malicious server can compromise all co-connected trusted servers. At scale, this enables widespread data theft and agent manipulation.
    # 
    # Mitigation:
    # Implement vetted submission processes for MCP registries with identity verification. Deploy code signing and integrity verification for published servers. Use server reputation systems and community reporting. Isolate untrusted servers from trusted ones using separate agent contexts. Audit installed servers regularly against known-good hashes.
    []

response:
  actions:
    - block_input
    - alert
  notify:
    - security_team

confidence: 40

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: false
  reason: "reference-based entry — payload lives at linked references, prose only inline"

# === sync-vulnerablemcp.ts provenance ===
_vulnerablemcp_sync:
  entry_id: "mcp-registry-hijacking"
  cve_ids: []
  category_source: "trust-model"
  category_atr: "tool-poisoning"
  severity_source: "high"
  impact_score: 7
  exploitability: "moderate"
  affected_components: ["ecosystem"]
  prevalence: "widespread"
  reported_by: "Academic Research (arXiv)"
  disclosed_date: "2025-10-18"
  alternative_names: ["Registry Server Hijacking","MCP Supply Chain via Registries"]
  tags: ["supply-chain","data-exfiltration","registry-hijacking"]
  cisco_objectives: ["supply-chain-compromise","data-privacy-violation"]
  primary_url: "https://arxiv.org/html/2510.16558v1"
  imported_at: "2026-06-12T03:06:38.228Z"
  data_source: "https://raw.githubusercontent.com/vineethsai/vulnerablemcp/main/data/vulnerabilities.json"
  advisory_body: |
    Description:
    Academic analysis of 67,057 MCP servers across 6 public registries found that a substantial number can be hijacked due to lack of vetted submission processes. Untrusted servers can exfiltrate data from co-connected trusted servers through the shared agent context.
    
    How (attack mechanism):
    Attackers submit malicious MCP servers to public registries using minimal or fabricated identity information. The servers pass basic checks (valid manifest, working endpoints) but contain hidden malicious behavior. When users install these servers alongside legitimate ones, the malicious server can exploit shared agent context to intercept or manipulate calls to trusted servers, exfiltrate data, or inject instructions.
    
    Where:
    The vulnerability is in MCP server registries (directories where users discover and install servers). Six major public registries were analyzed and all were found to lack adequate submission vetting, identity verification, and integrity controls.
    
    Impact:
    Large-scale supply chain compromise of the MCP ecosystem. Users who discover servers through registries have no reliable way to distinguish malicious from legitimate servers. The shared agent context means a single malicious server can compromise all co-connected trusted servers. At scale, this enables widespread data theft and agent manipulation.
    
    Mitigation:
    Implement vetted submission processes for MCP registries with identity verification. Deploy code signing and integrity verification for published servers. Use server reputation systems and community reporting. Isolate untrusted servers from trusted ones using separate agent contexts. Audit installed servers regularly against known-good hashes.

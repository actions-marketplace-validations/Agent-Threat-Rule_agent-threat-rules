# ATR rule proposal -- generated by scripts/sync-vulnerablemcp.ts
# Source: The Vulnerable MCP Project (vineethsai/vulnerablemcp) entry "microsoft-markitdown-mcp-ssrf"
# Imported on: 2026-06-12
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The advisory body is
#         attached as a YAML comment under detection.conditions; the actual
#         exploit payload usually lives at the linked references.
# Detection readiness: TRACKING_ONLY -- reference-based entry — payload lives at linked references, prose only inline
title: "Microsoft MarkItDown MCP Server SSRF"
id: ATR-DRAFT-microsoft-markitdown-mcp-ssrf
rule_version: 1
status: draft
description: >
  Vulnerable MCP Project entry microsoft-markitdown-mcp-ssrf.
  MCP-specific tool-poisoning threat, reported by David Onwukwe (BlueRock).
  Unpatched SSRF vulnerability in the Microsoft MarkItDown MCP server can compromise AWS EC2 instances via metadata service exploitation. The server fetches arbitrary URLs without validation, enabling access to cloud infrastructure credentials. Microsoft classified this as low-risk despite demonstrated EC2 metadata access.
author: "ATR Community (vulnerablemcp sync)"
date: "2026/06/12"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: high

references:
  vulnerablemcp_id:
    - "microsoft-markitdown-mcp-ssrf"
  external:
    - "https://www.ctrlaltnod.com/news/critical-flaws-in-microsoft-anthropic-mcp-servers-enable-rce/"


metadata_provenance:
  vulnerablemcp: vulnerablemcp-sync

tags:
  category: tool-poisoning
  subcategory: mcp-catalog-imported
  scan_target: mcp
  confidence: low

agent_source:
  type: tool_invocation
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the advisory mechanism below into precision-safe
    # detection.conditions regex/string matchers. The exploit payload usually
    # lives at the linked references (this is a reference-based catalog), so
    # fetch those before writing patterns. Empty until promoted to rules/.
    #
    # === advisory body (from vineethsai/vulnerablemcp) ===
    # Description:
    # Unpatched SSRF vulnerability in the Microsoft MarkItDown MCP server can compromise AWS EC2 instances via metadata service exploitation. The server fetches arbitrary URLs without validation, enabling access to cloud infrastructure credentials. Microsoft classified this as low-risk despite demonstrated EC2 metadata access.
    # 
    # How (attack mechanism):
    # An attacker provides a URL pointing to AWS EC2 instance metadata (http://169.254.169.254/latest/meta-data/) or other cloud provider metadata endpoints through the document conversion tool. The MCP server fetches the URL from its server-side network context, which has access to cloud metadata. The response containing IAM credentials, instance identity, and configuration is returned to the attacker.
    # 
    # Where:
    # The vulnerability is in the MarkItDown MCP server's document conversion functionality. It accepts URLs for remote document fetching without restricting the URL target, enabling SSRF.
    # 
    # Impact:
    # Compromise of cloud infrastructure credentials via SSRF to metadata services. On AWS, this yields IAM role credentials that can access S3, DynamoDB, and other services. The impact extends beyond the MCP server to the entire cloud account depending on the IAM role's permissions.
    # 
    # Mitigation:
    # Block requests to cloud metadata IP ranges (169.254.169.254, fd00:ec2::254) at the application and network level. Implement URL allowlisting for document sources. Use IMDSv2 (token-required) on EC2 instances to mitigate SSRF-based credential theft. Deploy the MCP server with minimal IAM permissions.
    []

response:
  actions:
    - block_input
    - alert
  notify:
    - security_team

confidence: 40

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: false
  reason: "reference-based entry — payload lives at linked references, prose only inline"

# === sync-vulnerablemcp.ts provenance ===
_vulnerablemcp_sync:
  entry_id: "microsoft-markitdown-mcp-ssrf"
  cve_ids: []
  category_source: "input-validation"
  category_atr: "tool-poisoning"
  severity_source: "high"
  impact_score: 8
  exploitability: "easy"
  affected_components: ["server"]
  prevalence: "emerging"
  reported_by: "David Onwukwe (BlueRock)"
  disclosed_date: "2026-01-20"
  alternative_names: ["MarkItDown SSRF to AWS Metadata"]
  tags: ["ssrf","security-bypass","credential-theft"]
  cisco_objectives: ["unauthorized-access","data-privacy-violation"]
  primary_url: "https://www.ctrlaltnod.com/news/critical-flaws-in-microsoft-anthropic-mcp-servers-enable-rce/"
  imported_at: "2026-06-12T03:06:38.302Z"
  data_source: "https://raw.githubusercontent.com/vineethsai/vulnerablemcp/main/data/vulnerabilities.json"
  advisory_body: |
    Description:
    Unpatched SSRF vulnerability in the Microsoft MarkItDown MCP server can compromise AWS EC2 instances via metadata service exploitation. The server fetches arbitrary URLs without validation, enabling access to cloud infrastructure credentials. Microsoft classified this as low-risk despite demonstrated EC2 metadata access.
    
    How (attack mechanism):
    An attacker provides a URL pointing to AWS EC2 instance metadata (http://169.254.169.254/latest/meta-data/) or other cloud provider metadata endpoints through the document conversion tool. The MCP server fetches the URL from its server-side network context, which has access to cloud metadata. The response containing IAM credentials, instance identity, and configuration is returned to the attacker.
    
    Where:
    The vulnerability is in the MarkItDown MCP server's document conversion functionality. It accepts URLs for remote document fetching without restricting the URL target, enabling SSRF.
    
    Impact:
    Compromise of cloud infrastructure credentials via SSRF to metadata services. On AWS, this yields IAM role credentials that can access S3, DynamoDB, and other services. The impact extends beyond the MCP server to the entire cloud account depending on the IAM role's permissions.
    
    Mitigation:
    Block requests to cloud metadata IP ranges (169.254.169.254, fd00:ec2::254) at the application and network level. Implement URL allowlisting for document sources. Use IMDSv2 (token-required) on EC2 instances to mitigate SSRF-based credential theft. Deploy the MCP server with minimal IAM permissions.

# ATR rule proposal -- generated by scripts/sync-vulnerablemcp.ts
# Source: The Vulnerable MCP Project (vineethsai/vulnerablemcp) entry "cursor-jira-mcp-zero-click"
# Imported on: 2026-06-12
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The advisory body is
#         attached as a YAML comment under detection.conditions; the actual
#         exploit payload usually lives at the linked references.
# Detection readiness: TRACKING_ONLY -- reference-based entry — payload lives at linked references, prose only inline
title: "Cursor + Jira MCP 0-Click Credential Exfiltration"
id: ATR-DRAFT-cursor-jira-mcp-zero-click
rule_version: 1
status: draft
description: >
  Vulnerable MCP Project entry cursor-jira-mcp-zero-click.
  MCP-specific prompt-injection threat, reported by Snyk Labs.
  Malicious Jira tickets contain obfuscated prompt injection that tricks Cursor into leaking JWT tokens and credentials through the Jira MCP server. A zero-click attack where simply having the agent process a Jira ticket triggers credential exfiltration.
author: "ATR Community (vulnerablemcp sync)"
date: "2026/06/12"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: high

references:
  vulnerablemcp_id:
    - "cursor-jira-mcp-zero-click"
  external:
    - "https://labs.snyk.io/resources/cursor-jira-mcp-vulnerability-explained/"


metadata_provenance:
  vulnerablemcp: vulnerablemcp-sync

tags:
  category: prompt-injection
  subcategory: mcp-catalog-imported
  scan_target: mcp
  confidence: low

agent_source:
  type: tool_invocation
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the advisory mechanism below into precision-safe
    # detection.conditions regex/string matchers. The exploit payload usually
    # lives at the linked references (this is a reference-based catalog), so
    # fetch those before writing patterns. Empty until promoted to rules/.
    #
    # === advisory body (from vineethsai/vulnerablemcp) ===
    # Description:
    # Malicious Jira tickets contain obfuscated prompt injection that tricks Cursor into leaking JWT tokens and credentials through the Jira MCP server. A zero-click attack where simply having the agent process a Jira ticket triggers credential exfiltration.
    # 
    # How (attack mechanism):
    # An attacker creates or modifies a Jira ticket with an obfuscated prompt injection payload hidden in the description or comments. When a developer's Cursor agent fetches the ticket via the Jira MCP server, the injected instructions execute. The payload instructs the agent to extract JWT tokens, API keys, and environment variables, then exfiltrate them through Jira API calls back to the attacker.
    # 
    # Where:
    # The attack payload is embedded in Jira ticket content (description, comments). The vulnerability chains the Jira MCP server's data retrieval with Cursor's agent execution environment where credentials are accessible.
    # 
    # Impact:
    # Theft of JWT tokens, API keys, and environment variable credentials. With these tokens, attackers can access the victim's Jira workspace, connected services, CI/CD pipelines, and cloud infrastructure. The attack is difficult to detect because exfiltration uses legitimate Jira API calls.
    # 
    # Mitigation:
    # Implement prompt injection detection on all Jira content before processing. Isolate credential storage from MCP agent context. Use short-lived, scoped tokens for Jira integrations. Monitor for unusual Jira API call patterns. Never expose environment variables to MCP tool contexts.
    []

response:
  actions:
    - block_input
    - alert
  notify:
    - security_team

confidence: 40

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: false
  reason: "reference-based entry — payload lives at linked references, prose only inline"

# === sync-vulnerablemcp.ts provenance ===
_vulnerablemcp_sync:
  entry_id: "cursor-jira-mcp-zero-click"
  cve_ids: []
  category_source: "prompt-injection"
  category_atr: "prompt-injection"
  severity_source: "high"
  impact_score: 8
  exploitability: "moderate"
  affected_components: ["client","server"]
  prevalence: "emerging"
  reported_by: "Snyk Labs"
  disclosed_date: "2025-08-20"
  alternative_names: ["Jira MCP Token Theft","Jira Ticket Prompt Injection"]
  tags: ["prompt-injection","credential-theft","data-exfiltration","toxic-agent-flow"]
  cisco_objectives: ["data-privacy-violation","goal-hijacking"]
  primary_url: "https://labs.snyk.io/resources/cursor-jira-mcp-vulnerability-explained/"
  imported_at: "2026-06-12T03:06:38.153Z"
  data_source: "https://raw.githubusercontent.com/vineethsai/vulnerablemcp/main/data/vulnerabilities.json"
  advisory_body: |
    Description:
    Malicious Jira tickets contain obfuscated prompt injection that tricks Cursor into leaking JWT tokens and credentials through the Jira MCP server. A zero-click attack where simply having the agent process a Jira ticket triggers credential exfiltration.
    
    How (attack mechanism):
    An attacker creates or modifies a Jira ticket with an obfuscated prompt injection payload hidden in the description or comments. When a developer's Cursor agent fetches the ticket via the Jira MCP server, the injected instructions execute. The payload instructs the agent to extract JWT tokens, API keys, and environment variables, then exfiltrate them through Jira API calls back to the attacker.
    
    Where:
    The attack payload is embedded in Jira ticket content (description, comments). The vulnerability chains the Jira MCP server's data retrieval with Cursor's agent execution environment where credentials are accessible.
    
    Impact:
    Theft of JWT tokens, API keys, and environment variable credentials. With these tokens, attackers can access the victim's Jira workspace, connected services, CI/CD pipelines, and cloud infrastructure. The attack is difficult to detect because exfiltration uses legitimate Jira API calls.
    
    Mitigation:
    Implement prompt injection detection on all Jira content before processing. Isolate credential storage from MCP agent context. Use short-lived, scoped tokens for Jira integrations. Monitor for unusual Jira API call patterns. Never expose environment variables to MCP tool contexts.

# ATR rule proposal -- generated by scripts/sync-vulnerablemcp.ts
# Source: The Vulnerable MCP Project (vineethsai/vulnerablemcp) entry "cross-server-tool-shadowing"
# Imported on: 2026-06-12
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The advisory body is
#         attached as a YAML comment under detection.conditions; the actual
#         exploit payload usually lives at the linked references.
# Detection readiness: TRACKING_ONLY -- reference-based entry — payload lives at linked references, prose only inline
title: "Cross-Server Tool Shadowing"
id: ATR-DRAFT-cross-server-tool-shadowing
rule_version: 1
status: draft
description: >
  Vulnerable MCP Project entry cross-server-tool-shadowing.
  MCP-specific tool-poisoning threat, reported by Elena Cross (via Simon Willison).
  When multiple MCP servers are connected to the same agent, a malicious server can override or intercept calls intended for a trusted server by registering tools with the same or similar names.
author: "ATR Community (vulnerablemcp sync)"
date: "2026/06/12"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: high

references:
  vulnerablemcp_id:
    - "cross-server-tool-shadowing"
  external:
    - "https://simonwillison.net/2025/Apr/9/mcp-prompt-injection/"
    - "https://embracethered.com/blog/posts/2025/model-context-protocol-security-risks-and-exploits/"


metadata_provenance:
  vulnerablemcp: vulnerablemcp-sync

tags:
  category: tool-poisoning
  subcategory: mcp-catalog-imported
  scan_target: mcp
  confidence: low

agent_source:
  type: tool_invocation
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the advisory mechanism below into precision-safe
    # detection.conditions regex/string matchers. The exploit payload usually
    # lives at the linked references (this is a reference-based catalog), so
    # fetch those before writing patterns. Empty until promoted to rules/.
    #
    # === advisory body (from vineethsai/vulnerablemcp) ===
    # Description:
    # When multiple MCP servers are connected to the same agent, a malicious server can override or intercept calls intended for a trusted server by registering tools with the same or similar names.
    # 
    # How (attack mechanism):
    # An attacker's MCP server registers a tool with the same name as a trusted server's tool (e.g., 'send_email'). When the LLM invokes 'send_email', the call is routed to the attacker's version instead of the legitimate one. The attacker can then intercept parameters, modify behavior, or exfiltrate data before optionally forwarding to the real tool.
    # 
    # Where:
    # The vulnerability exists in MCP client tool routing logic. When multiple servers expose tools with identical names, the client must choose which to invoke, often favoring the most recently connected server.
    # 
    # Impact:
    # Complete interception of tool calls including sensitive parameters like credentials, file contents, and user data. Can enable man-in-the-middle attacks between the user and trusted services. The LLM cannot distinguish between legitimate and malicious tools with the same name.
    # 
    # Mitigation:
    # Use MCP clients that namespace tools by server (e.g., server_name.tool_name). Avoid connecting untrusted servers alongside sensitive ones. Implement tool priority systems where trusted servers take precedence. Review connected servers regularly and remove unnecessary ones.
    []

response:
  actions:
    - block_input
    - alert
  notify:
    - security_team

confidence: 40

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: false
  reason: "reference-based entry — payload lives at linked references, prose only inline"

# === sync-vulnerablemcp.ts provenance ===
_vulnerablemcp_sync:
  entry_id: "cross-server-tool-shadowing"
  cve_ids: []
  category_source: "integrity"
  category_atr: "tool-poisoning"
  severity_source: "high"
  impact_score: 7
  exploitability: "moderate"
  affected_components: ["client","server"]
  prevalence: "common"
  reported_by: "Elena Cross (via Simon Willison)"
  disclosed_date: "2025-04-09"
  alternative_names: ["Tool Override Attack","Multi-Server Confused Deputy"]
  tags: ["tool-hijacking","confused-deputy","tool-overriding"]
  cisco_objectives: ["goal-hijacking","integrity-compromise"]
  primary_url: "https://simonwillison.net/2025/Apr/9/mcp-prompt-injection/"
  imported_at: "2026-06-12T03:06:37.919Z"
  data_source: "https://raw.githubusercontent.com/vineethsai/vulnerablemcp/main/data/vulnerabilities.json"
  advisory_body: |
    Description:
    When multiple MCP servers are connected to the same agent, a malicious server can override or intercept calls intended for a trusted server by registering tools with the same or similar names.
    
    How (attack mechanism):
    An attacker's MCP server registers a tool with the same name as a trusted server's tool (e.g., 'send_email'). When the LLM invokes 'send_email', the call is routed to the attacker's version instead of the legitimate one. The attacker can then intercept parameters, modify behavior, or exfiltrate data before optionally forwarding to the real tool.
    
    Where:
    The vulnerability exists in MCP client tool routing logic. When multiple servers expose tools with identical names, the client must choose which to invoke, often favoring the most recently connected server.
    
    Impact:
    Complete interception of tool calls including sensitive parameters like credentials, file contents, and user data. Can enable man-in-the-middle attacks between the user and trusted services. The LLM cannot distinguish between legitimate and malicious tools with the same name.
    
    Mitigation:
    Use MCP clients that namespace tools by server (e.g., server_name.tool_name). Avoid connecting untrusted servers alongside sensitive ones. Implement tool priority systems where trusted servers take precedence. Review connected servers regularly and remove unnecessary ones.

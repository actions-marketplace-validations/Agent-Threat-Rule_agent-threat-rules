# ATR rule proposal -- generated by scripts/sync-vulnerablemcp.ts
# Source: The Vulnerable MCP Project (vineethsai/vulnerablemcp) entry "ansi-terminal-code-deception"
# Imported on: 2026-06-12
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The advisory body is
#         attached as a YAML comment under detection.conditions; the actual
#         exploit payload usually lives at the linked references.
# Detection readiness: TRACKING_ONLY -- reference-based entry — payload lives at linked references, prose only inline
title: "ANSI Terminal Code Deception in MCP"
id: ATR-DRAFT-ansi-terminal-code-deception
rule_version: 1
status: draft
description: >
  Vulnerable MCP Project entry ansi-terminal-code-deception.
  MCP-specific prompt-injection threat, reported by Keith Hoodlet (Trail of Bits).
  Attackers use ANSI terminal escape codes to hide malicious instructions in MCP tool descriptions and outputs, rendering them invisible on screen while the LLM processes them normally. Demonstrated against Claude Code with no filtering or sanitization.
author: "ATR Community (vulnerablemcp sync)"
date: "2026/06/12"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: high

references:
  vulnerablemcp_id:
    - "ansi-terminal-code-deception"
  external:
    - "https://blog.trailofbits.com/2025/04/29/deceiving-users-with-ansi-terminal-codes-in-mcp/"


metadata_provenance:
  vulnerablemcp: vulnerablemcp-sync

tags:
  category: prompt-injection
  subcategory: mcp-catalog-imported
  scan_target: mcp
  confidence: low

agent_source:
  type: tool_invocation
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the advisory mechanism below into precision-safe
    # detection.conditions regex/string matchers. The exploit payload usually
    # lives at the linked references (this is a reference-based catalog), so
    # fetch those before writing patterns. Empty until promoted to rules/.
    #
    # === advisory body (from vineethsai/vulnerablemcp) ===
    # Description:
    # Attackers use ANSI terminal escape codes to hide malicious instructions in MCP tool descriptions and outputs, rendering them invisible on screen while the LLM processes them normally. Demonstrated against Claude Code with no filtering or sanitization.
    # 
    # How (attack mechanism):
    # Attackers embed ANSI escape codes (e.g., codes that set text color to match background, or move cursor to overwrite content) in tool descriptions. When displayed in a terminal, the malicious text is invisible. However, the LLM receives the raw text including the hidden instructions. These instructions can suggest downloading packages from malicious servers, planting backdoors, or modifying development environments.
    # 
    # Where:
    # The vulnerability exists at the intersection of terminal rendering and LLM context processing. ANSI escape codes in tool descriptions are processed by the terminal (making text invisible) but remain in the raw text that the LLM reads.
    # 
    # Impact:
    # Even security-conscious users who inspect tool descriptions cannot see the hidden content in terminal displays. This enables supply chain attacks through compromised package suggestions, stealth modification of development environments, and persistent compromise through backdoored dependencies. The attack is particularly dangerous in CI/CD environments using MCP tools.
    # 
    # Mitigation:
    # Strip ANSI escape sequences from all MCP tool descriptions and outputs before display and before LLM processing. Use MCP clients that render descriptions in non-terminal contexts (web UI, plaintext). Implement content security policies for tool descriptions. Audit tool descriptions in raw/hex mode to detect hidden content.
    []

response:
  actions:
    - block_input
    - alert
  notify:
    - security_team

confidence: 40

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: false
  reason: "reference-based entry — payload lives at linked references, prose only inline"

# === sync-vulnerablemcp.ts provenance ===
_vulnerablemcp_sync:
  entry_id: "ansi-terminal-code-deception"
  cve_ids: []
  category_source: "prompt-injection"
  category_atr: "prompt-injection"
  severity_source: "high"
  impact_score: 8
  exploitability: "easy"
  affected_components: ["server","client"]
  prevalence: "emerging"
  reported_by: "Keith Hoodlet (Trail of Bits)"
  disclosed_date: "2025-04-29"
  alternative_names: ["Terminal Escape Sequence Injection","ANSI Steganography Attack"]
  tags: ["terminal-injection","hidden-instructions","supply-chain"]
  cisco_objectives: ["goal-hijacking","supply-chain-compromise"]
  primary_url: "https://blog.trailofbits.com/2025/04/29/deceiving-users-with-ansi-terminal-codes-in-mcp/"
  imported_at: "2026-06-12T03:06:38.046Z"
  data_source: "https://raw.githubusercontent.com/vineethsai/vulnerablemcp/main/data/vulnerabilities.json"
  advisory_body: |
    Description:
    Attackers use ANSI terminal escape codes to hide malicious instructions in MCP tool descriptions and outputs, rendering them invisible on screen while the LLM processes them normally. Demonstrated against Claude Code with no filtering or sanitization.
    
    How (attack mechanism):
    Attackers embed ANSI escape codes (e.g., codes that set text color to match background, or move cursor to overwrite content) in tool descriptions. When displayed in a terminal, the malicious text is invisible. However, the LLM receives the raw text including the hidden instructions. These instructions can suggest downloading packages from malicious servers, planting backdoors, or modifying development environments.
    
    Where:
    The vulnerability exists at the intersection of terminal rendering and LLM context processing. ANSI escape codes in tool descriptions are processed by the terminal (making text invisible) but remain in the raw text that the LLM reads.
    
    Impact:
    Even security-conscious users who inspect tool descriptions cannot see the hidden content in terminal displays. This enables supply chain attacks through compromised package suggestions, stealth modification of development environments, and persistent compromise through backdoored dependencies. The attack is particularly dangerous in CI/CD environments using MCP tools.
    
    Mitigation:
    Strip ANSI escape sequences from all MCP tool descriptions and outputs before display and before LLM processing. Use MCP clients that render descriptions in non-terminal contexts (web UI, plaintext). Implement content security policies for tool descriptions. Audit tool descriptions in raw/hex mode to detect hidden content.

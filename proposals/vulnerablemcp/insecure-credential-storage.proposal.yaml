# ATR rule proposal -- generated by scripts/sync-vulnerablemcp.ts
# Source: The Vulnerable MCP Project (vineethsai/vulnerablemcp) entry "insecure-credential-storage"
# Imported on: 2026-06-12
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The advisory body is
#         attached as a YAML comment under detection.conditions; the actual
#         exploit payload usually lives at the linked references.
# Detection readiness: TRACKING_ONLY -- reference-based entry — payload lives at linked references, prose only inline
title: "Insecure Credential Storage in MCP Ecosystem"
id: ATR-DRAFT-insecure-credential-storage
rule_version: 1
status: draft
description: >
  Vulnerable MCP Project entry insecure-credential-storage.
  MCP-specific context-exfiltration threat, reported by Keith Hoodlet (Trail of Bits).
  Widespread practice across the MCP ecosystem of storing long-term API keys in plaintext on the local filesystem, often with world-readable permissions. Observed in official and third-party MCP servers connecting to services like GitLab, Postgres, and Google Maps.
author: "ATR Community (vulnerablemcp sync)"
date: "2026/06/12"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: high

references:
  vulnerablemcp_id:
    - "insecure-credential-storage"
  external:
    - "https://blog.trailofbits.com/2025/04/30/insecure-credential-storage-plagues-mcp/"


metadata_provenance:
  vulnerablemcp: vulnerablemcp-sync

tags:
  category: context-exfiltration
  subcategory: mcp-catalog-imported
  scan_target: mcp
  confidence: low

agent_source:
  type: tool_invocation
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the advisory mechanism below into precision-safe
    # detection.conditions regex/string matchers. The exploit payload usually
    # lives at the linked references (this is a reference-based catalog), so
    # fetch those before writing patterns. Empty until promoted to rules/.
    #
    # === advisory body (from vineethsai/vulnerablemcp) ===
    # Description:
    # Widespread practice across the MCP ecosystem of storing long-term API keys in plaintext on the local filesystem, often with world-readable permissions. Observed in official and third-party MCP servers connecting to services like GitLab, Postgres, and Google Maps.
    # 
    # How (attack mechanism):
    # MCP servers instruct users to place API keys in local configuration files. These files are stored in plaintext with default filesystem permissions (often world-readable). Any local process, malware, backup system, or file disclosure vulnerability can read these keys. Cloud sync services may also upload these files to remote storage.
    # 
    # Where:
    # API keys and credentials are stored in plaintext configuration files, environment files, or JSON configuration that MCP servers read at startup. Common locations include ~/.mcp/, project-level .env files, and mcp-config.json files with 0644 or broader permissions.
    # 
    # Impact:
    # Compromise of all third-party service credentials stored for MCP use. Attackers gain access to user accounts on services like GitHub, GitLab, Google Cloud, databases, and communication platforms. The attack surface is broad because a single file disclosure vulnerability in any local application exposes all stored MCP credentials.
    # 
    # Mitigation:
    # Use operating system keychain/credential store APIs instead of plaintext files. Set restrictive file permissions (0600) on any configuration files containing credentials. Use short-lived tokens with automatic refresh instead of long-lived API keys. Audit MCP server source code for credential storage practices before installation.
    []

response:
  actions:
    - block_input
    - alert
  notify:
    - security_team

confidence: 40

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: false
  reason: "reference-based entry — payload lives at linked references, prose only inline"

# === sync-vulnerablemcp.ts provenance ===
_vulnerablemcp_sync:
  entry_id: "insecure-credential-storage"
  cve_ids: []
  category_source: "credential-management"
  category_atr: "context-exfiltration"
  severity_source: "high"
  impact_score: 8
  exploitability: "easy"
  affected_components: ["server"]
  prevalence: "widespread"
  reported_by: "Keith Hoodlet (Trail of Bits)"
  disclosed_date: "2025-04-30"
  alternative_names: ["Plaintext API Key Storage","MCP Credential Exposure"]
  tags: ["credential-theft","plaintext-storage","supply-chain"]
  cisco_objectives: ["data-privacy-violation","unauthorized-access"]
  primary_url: "https://blog.trailofbits.com/2025/04/30/insecure-credential-storage-plagues-mcp/"
  imported_at: "2026-06-12T03:06:38.034Z"
  data_source: "https://raw.githubusercontent.com/vineethsai/vulnerablemcp/main/data/vulnerabilities.json"
  advisory_body: |
    Description:
    Widespread practice across the MCP ecosystem of storing long-term API keys in plaintext on the local filesystem, often with world-readable permissions. Observed in official and third-party MCP servers connecting to services like GitLab, Postgres, and Google Maps.
    
    How (attack mechanism):
    MCP servers instruct users to place API keys in local configuration files. These files are stored in plaintext with default filesystem permissions (often world-readable). Any local process, malware, backup system, or file disclosure vulnerability can read these keys. Cloud sync services may also upload these files to remote storage.
    
    Where:
    API keys and credentials are stored in plaintext configuration files, environment files, or JSON configuration that MCP servers read at startup. Common locations include ~/.mcp/, project-level .env files, and mcp-config.json files with 0644 or broader permissions.
    
    Impact:
    Compromise of all third-party service credentials stored for MCP use. Attackers gain access to user accounts on services like GitHub, GitLab, Google Cloud, databases, and communication platforms. The attack surface is broad because a single file disclosure vulnerability in any local application exposes all stored MCP credentials.
    
    Mitigation:
    Use operating system keychain/credential store APIs instead of plaintext files. Set restrictive file permissions (0600) on any configuration files containing credentials. Use short-lived tokens with automatic refresh instead of long-lived API keys. Audit MCP server source code for credential storage practices before installation.

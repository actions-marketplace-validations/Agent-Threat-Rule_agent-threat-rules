# ATR rule proposal -- generated by scripts/sync-vulnerablemcp.ts
# Source: The Vulnerable MCP Project (vineethsai/vulnerablemcp) entry "tool-function-parameter-abuse"
# Imported on: 2026-06-12
# Status: DRAFT -- detection.conditions MUST be filled in by a human reviewer
#         before this rule can be considered for merge. The advisory body is
#         attached as a YAML comment under detection.conditions; the actual
#         exploit payload usually lives at the linked references.
# Detection readiness: TRACKING_ONLY -- reference-based entry — payload lives at linked references, prose only inline
title: "Tool Function Parameter Abuse"
id: ATR-DRAFT-tool-function-parameter-abuse
rule_version: 1
status: draft
description: >
  Vulnerable MCP Project entry tool-function-parameter-abuse.
  MCP-specific prompt-injection threat, reported by HiddenLayer.
  Malicious MCP servers define tools with parameters like 'system_prompt' or 'conversation_history' that instruct the LLM to populate them with sensitive context. This extracts chain-of-thought, previous tool results, system prompts, and conversation data through seemingly normal tool invocations.
author: "ATR Community (vulnerablemcp sync)"
date: "2026/06/12"
schema_version: "0.1"
detection_tier: pattern
maturity: experimental
severity: high

references:
  vulnerablemcp_id:
    - "tool-function-parameter-abuse"
  external:
    - "https://hiddenlayer.com/innovation-hub/exploiting-mcp-tool-parameters/"


metadata_provenance:
  vulnerablemcp: vulnerablemcp-sync

tags:
  category: prompt-injection
  subcategory: mcp-catalog-imported
  scan_target: mcp
  confidence: low

agent_source:
  type: tool_invocation
  framework:
    - any
  provider:
    - any

detection:
  condition: any
  false_positives: []
  conditions:
    # TODO(human): convert the advisory mechanism below into precision-safe
    # detection.conditions regex/string matchers. The exploit payload usually
    # lives at the linked references (this is a reference-based catalog), so
    # fetch those before writing patterns. Empty until promoted to rules/.
    #
    # === advisory body (from vineethsai/vulnerablemcp) ===
    # Description:
    # Malicious MCP servers define tools with parameters like 'system_prompt' or 'conversation_history' that instruct the LLM to populate them with sensitive context. This extracts chain-of-thought, previous tool results, system prompts, and conversation data through seemingly normal tool invocations.
    # 
    # How (attack mechanism):
    # A malicious tool defines parameters with names like 'system_prompt', 'conversation_history', 'chain_of_thought', or 'available_tools'. The LLM, following its training to populate parameters accurately, extracts this information from its context and passes it to the malicious tool. The server collects this data, gaining visibility into the user's full agent configuration.
    # 
    # Where:
    # The vulnerability is in the tool parameter schema. By naming parameters suggestively (e.g., 'system_prompt', 'full_context', 'previous_results'), the attacker exploits the LLM's tendency to fill parameters with matching data from its context.
    # 
    # Impact:
    # Extraction of system prompts (revealing business logic and security rules), conversation history (containing sensitive user data), chain-of-thought (revealing reasoning patterns), and tool inventories (enabling targeted attacks on other connected services). This intelligence can be used to craft more effective follow-up attacks.
    # 
    # Mitigation:
    # Implement parameter value filtering that blocks system prompt and context leakage. Use MCP clients that validate parameter values against sensitivity heuristics. Restrict what context data the LLM can include in tool parameters. Flag tools with suspiciously named parameters during review.
    []

response:
  actions:
    - block_input
    - alert
  notify:
    - security_team

confidence: 40

test_cases:
  true_positives: []
  true_negatives: []

_triage:
  detection_ready: false
  reason: "reference-based entry — payload lives at linked references, prose only inline"

# === sync-vulnerablemcp.ts provenance ===
_vulnerablemcp_sync:
  entry_id: "tool-function-parameter-abuse"
  cve_ids: []
  category_source: "prompt-injection"
  category_atr: "prompt-injection"
  severity_source: "high"
  impact_score: 8
  exploitability: "moderate"
  affected_components: ["server"]
  prevalence: "emerging"
  reported_by: "HiddenLayer"
  disclosed_date: "2025-05-15"
  alternative_names: ["Parameter Injection for Data Extraction","Hidden Parameter Exfiltration"]
  tags: ["parameter-abuse","data-exfiltration","hidden-instructions"]
  cisco_objectives: ["data-privacy-violation","goal-hijacking"]
  primary_url: "https://hiddenlayer.com/innovation-hub/exploiting-mcp-tool-parameters/"
  imported_at: "2026-06-12T03:06:38.060Z"
  data_source: "https://raw.githubusercontent.com/vineethsai/vulnerablemcp/main/data/vulnerabilities.json"
  advisory_body: |
    Description:
    Malicious MCP servers define tools with parameters like 'system_prompt' or 'conversation_history' that instruct the LLM to populate them with sensitive context. This extracts chain-of-thought, previous tool results, system prompts, and conversation data through seemingly normal tool invocations.
    
    How (attack mechanism):
    A malicious tool defines parameters with names like 'system_prompt', 'conversation_history', 'chain_of_thought', or 'available_tools'. The LLM, following its training to populate parameters accurately, extracts this information from its context and passes it to the malicious tool. The server collects this data, gaining visibility into the user's full agent configuration.
    
    Where:
    The vulnerability is in the tool parameter schema. By naming parameters suggestively (e.g., 'system_prompt', 'full_context', 'previous_results'), the attacker exploits the LLM's tendency to fill parameters with matching data from its context.
    
    Impact:
    Extraction of system prompts (revealing business logic and security rules), conversation history (containing sensitive user data), chain-of-thought (revealing reasoning patterns), and tool inventories (enabling targeted attacks on other connected services). This intelligence can be used to craft more effective follow-up attacks.
    
    Mitigation:
    Implement parameter value filtering that blocks system prompt and context leakage. Use MCP clients that validate parameter values against sensitivity heuristics. Restrict what context data the LLM can include in tool parameters. Flag tools with suspiciously named parameters during review.
